# QI-D F4 exceptional finite certificate — 2026-09-09

Source authority: `cmbant/QIprojects` main at `acfb127df5cc5998fb91cd00ea6e5311d1462e39`.

Canonical source files:

- `QI-D/code/certify_F4_exceptional_facet.py`
- `QI-D/verification/certify_F4_exceptional_facet.txt`
- `QI-D/notes/T33_REPORT.md`

Lean modules:

- `FormalResearch/QID/F4ExceptionalFacetCertificate.lean`
- `FormalResearch/QID/F4ExceptionalFacetAffineWitness.lean`

Maintained aggregate import added to `FormalResearch.lean`.

## Exact Lean-checked content

The parameterized finite certificate checks the exact finite set generated by the two displayed F4 generators using a three-coset parameterization over the 384 signed-permutation subgroup. Lean checks:

- all `3 * 384 = 1152` parameters are reachable by source-generator words;
- the 1152 parameters give distinct exact matrices;
- both finite transition formulas agree exactly with right multiplication by the displayed source generators;
- the displayed rank-one normal has support `1` with `288` active matrices;
- the displayed exceptional rank-three normal has support `1` with `36` active matrices;
- the exceptional normal is rank three via a nonzero `3 x 3` minor while its `4 x 4` determinant vanishes;
- the simple-root inverse, folded E6/F4 coordinate conversion, and folded Gram relation are exact.

The affine-witness module checks 16 explicit support-active generator words. After subtracting one base point and projecting to 15 coordinates, Lean checks an explicit two-sided inverse of the resulting `15 x 15` rational difference matrix and proves the corresponding coefficient kernel is trivial. This is the exact affine-rank-15 witness used for the displayed supporting facet.

## Verification

Exact Lean-content head before validation-workflow-only commits:

`16ce743be5cd276410c905f33aadc2699d6a12e3`

GitHub Actions run:

`34399984280`

The run passed:

1. placeholder rejection;
2. `lake build FormalResearch.QID.F4ExceptionalFacetCertificate`;
3. `lake build FormalResearch.QID.F4ExceptionalFacetAffineWitness`;
4. `lake build FormalResearch`.

The workflow-only validation head was `23c1cde9d3c7c8a8e24d447ee758d0aa974353f3`. The temporary workflow was removed after the green run; this cleanup does not change the verified Lean source bytes.

## Scope / trust boundary

This does **not** prove compact-F4 simultaneous Weyl convexity, does not prove that every compact-F4 Cartan compression lies in `conv W(F4)`, and does not prove the literature classification that there are exactly two F4 facet orbits. Those remain external/open as stated in the QI-D source.

What is Lean-checked here is the concrete finite F4 certificate for the displayed exceptional normal, including support, active count, rank, folded coordinates, and an explicit affine-rank-15 active witness.

# Formal research handoff — 2026-09-10

This supersedes the restart/status portions of `HANDOFF_2026-09-09.md` and earlier handoffs. The programme moved materially again: QI-D's exceptional F4 certificate and E6/F4 A2 quartic reduction are now merged and aggregate-green in Lean, QIprojects has integrated the all-rank type-D theorem into the manuscript, T48 has completed at a reduced-locus/zero-sector boundary stop condition, and T49 now owns the unresolved exceptional interior.

## Current authoritative heads

Research/manuscript/certificate authority:

- repository: `cmbant/QIprojects:main`
- current live head after QIprojects PR #87: `e2b93eda6ca2d1a9d9bf5ed280946c6b5cc71b25`

Lean authority:

- repository: `cmbant/reasoner:formal-research-lean`
- current maintained head after reasoner PR #18: `e32fd5a27602e5972b2258fd7159d33cb06a7d2f`

Re-audit both live heads before starting new work.

## Exact maintained Lean verification frontier

The newest promoted feature is the QI-D T33 E6/F4 `A2` envelope-to-quartic algebra, reasoner PR #18.

Exact promoted Lean-content head:

`8108be5de50e40a6daa05b6004d63a87c388ba95`

GitHub Actions promotion run:

`34406631515`

That run passed:

1. rejection of `sorry` / `admit`;
2. `lake build FormalResearch.QID.E6A2PhaseEnvelopeQuartic`;
3. full `lake build FormalResearch`.

PR #18 then merged the same Lean bytes, after temporary-workflow cleanup and status-note addition, into `formal-research-lean` at `e32fd5a27602e5972b2258fd7159d33cb06a7d2f`.

Earlier merged features retain their own exact verification evidence:

- PR #8, Blaschke quartic reconstruction bridge: head `1dc24e93c1483782dd80b162c9b48cd26707fc41`, run `34336956345`;
- PR #9, QI-A `d=3` local-Schur overhead: head `2e15ff34467c5de1a2432c6d6bd7c5c7ddad19ed`, run `34339736666`;
- PR #10, QI-A information-hierarchy coefficients: head `3591192788ae917bb43c1f8e7102e95270258efb`, run `34347204321`;
- PR #11, QI-B symmetric `U(1)` exponent-gap certificate: head `297231d4be4780b1d091092a7d841bbbe62edb85`, run `34348733331`;
- PR #12, QI-D D4 finite witness/dual: head `8a95d5ba7fb0b200dfbfb2efb4c97e3710ead8be`, run `34374566836`;
- PR #13, QI-D nested all-rank lower witness: head `a8d883584eb235fa2f1c4af0c01ec309cedfc0ac`, run `34378799353`;
- PR #14, QI-D nested all-rank upper decomposition/support implication: head `df4466a9e5b773db105bb1fcbd14945129d2a33b`, run `34390385283`;
- PR #16, QI-D exceptional F4 finite facet certificate: head `16ce743be5cd276410c905f33aadc2699d6a12e3`, run `34399984280`;
- PR #18, QI-D E6/F4 A2 envelope-to-quartic algebra: head `8108be5de50e40a6daa05b6004d63a87c388ba95`, run `34406631515`.

All promoted runs above include a full aggregate build at the cited exact Lean-content head.

## QIprojects planning/status authority

QIprojects PR #87 merged the post-E6/T48 Lean-plan reconciliation into `main`. Read these before choosing a target:

- `docs/lean_status.md`;
- `docs/agent_tasks/T20_REPORT.md`;
- `docs/agent_tasks/T20_lean_small_targets.md`.

Those files now supersede the old post-PR14 queue. In particular, F4 and the T33 E6 quartic are completed Lean targets, T48 is not an unlaunched research task, and T49 is not yet a source-proved Lean completion target.

## Paper-integration checkpoint rule

If source-proved / Lean-checked work is materially stronger than the current manuscript TeX, do **not** silently edit or merge the paper. Create a QIprojects review branch and open a **draft/unmerged PR** containing:

1. the exact source theorem/certificate and source pin;
2. the exact reasoner verification head/run/merge PR;
3. the Lean trust boundary;
4. proposed manuscript wording / integration location;
5. a validation checklist.

Current open draft checkpoints:

- QIprojects PR #84: reproducibility text for the displayed exceptional F4 finite facet certificate, without changing the open compact-F4 theorem status;
- QIprojects PR #86: reproducibility text for the E6/F4 A2 one-phase/quartic reduction, without claiming uniform compact-endpoint positivity.

Both are intentionally draft and unmerged. Do not merge either automatically.

QIprojects PR #82 is no longer a pending checkpoint: it has merged the all-rank nested type-D equality into the current manuscript.

## Blaschke status

The degree-four charge gap is source-proved with restartable exact CAS reconstruction. Reasoner PR #8 added `FormalResearch/Blaschke/QuarticChargeGapReconstructionBridge.lean` and the maintained aggregate contains both the quartic algebra and the reconstruction/sign/unsquaring bridge.

Lean proves the conditional scalar chain ending in `1/2 <= mu1-mu2` from the exact reconstruction equality and analytic scalar hypotheses. The raw CAS derivation, Bessel/Gram input, Schur-domain theorem, Möbius normalization, residue/Hilbert-metric construction and confluent-continuity step remain external. Do not call the entire analytic proof internally Lean-derived.

Primitive all-degree Gate A, Gate B, degree-five Blaschke Hodge stability and other source-open claims remain open unless live QIprojects says otherwise.

## QI-A reconciliation

QI-A remains the live submission-facing copy-complexity / quantum-memory manuscript.

The earlier audit remains valid through current QIprojects head `e2b93eda...`: the full `acfb127d... -> e2b93eda...` source delta contains no `QI-A/` file changes.

Important source correction retained: the hoped-for universal unrestricted-physical block-curve converse is false. Current source separates strong reconstructive/full-block, deferred-query full-block, unrestricted physical with free classical side information, and sector-label-only physical memory.

Merged Lean coverage includes:

- exact three-copy `d=3` local-Schur character identities (`6K=L`, with exact endpoint corrections), PR #9;
- exact scalar information-hierarchy/Chernoff coefficient ordering, PR #10.

Both are already represented in current QI-A TeX; no paper-review PR is needed for those results.

T07 finite coding/guessing statements are legitimate future targets only if their message/query/guessing hypotheses are represented faithfully. The reversible coherent-width theorem still requires substantial recovery/entanglement/Schmidt-number infrastructure.

## QI-C reconciliation

QI-C remains a research/provenance/certificate package. The earlier audit also remains valid through current QIprojects head `e2b93eda...`: the full `acfb127d... -> e2b93eda...` delta contains no `QI-C/` file changes.

The canonical all-`m` qubit-tail source `QI-C/code/verify_qubit_ci_tail_proof.py` remains unchanged from the earlier exact blob audit (`e5d44a1733ce0520bebd37a9172348b1476f7463`).

Therefore Endpoint14 maintenance remains closed. The current Lean chain formalizes the determinant/nonvanishing certificate layer; do not identify that automatically with the full geometric `d_loc=3` local-identifiability theorem.

## QI-D: all-rank type-D theorem

Reasoner PRs #12–#14 formalize the finite D4 certificate plus the exact lower and upper all-rank matrix algebra.

For rank `n=k+4`, Lean defines the actual nested witness and proves `<N_n,A_n>=n+2/3`; it also proves the exact source upper rank-one decomposition and derives `<N_n,T><=n+2/3` from the individual sharp atom-support inequalities.

The complete semantic theorem still depends on explicit external bridges:

1. generalized-Birkhoff `DS(W(D_n))` membership/nesting;
2. the general sharp real type-D rank-one support/Procrustes theorem.

QIprojects PR #82 has now merged the all-rank equality into `QI-D/paper/type_d_weyl_convexity.tex`, together with a source-side dominant-representative support argument for the specific atom shapes used. That paper proof does not mean the general support theorem or semantic DS bridge is Lean-formalized.

## QI-D T33 exceptional F4 certificate — merged PR #16

Canonical source:

- `QI-D/code/certify_F4_exceptional_facet.py`;
- `QI-D/verification/certify_F4_exceptional_facet.txt`;
- `QI-D/notes/T33_REPORT.md`.

Lean modules:

- `FormalResearch/QID/F4ExceptionalFacetCertificate.lean`;
- `FormalResearch/QID/F4ExceptionalFacetAffineWitness.lean`.

Lean checks:

- all `1152` exact source-generated Weyl matrices via a three-coset parameterization;
- exact generator transitions and distinctness;
- rank-one support `1`, active count `288`;
- exceptional rank-three support `1`, active count `36`;
- exact folded E6/F4 coordinate and Gram identities;
- an explicit 16-point active witness giving affine rank `15`.

External/open inputs remain explicit: the literature two-facet-orbit classification and compact-F4 simultaneous Weyl convexity. Do **not** infer a compact-F4 theorem from PR #16.

## QI-D T33 E6/F4 A2 envelope-to-quartic — merged PR #18

Canonical source:

- `QI-D/code/certify_E6_A2_phase_envelope.py`;
- `QI-D/verification/certify_E6_A2_phase_envelope.txt`;
- `QI-D/notes/EXCEPTIONAL_COMPACT_COMPRESSION.md`.

The source verifier is unchanged from the Lean implementation audit at QIprojects `acfb127df5cc5998fb91cd00ea6e5311d1462e39` through the current source line; later changes concern QI-D manuscript/planning/Weyl work rather than this certificate.

Lean module:

`FormalResearch/QID/E6A2PhaseEnvelopeQuartic.lean`.

Lean checks the exact rational algebraic layer:

- determinant `3` and the displayed inverse of the A2 phase map;
- tangent-half-angle numerator and denominator identities;
- exact clearing of `(1+t^2)^2` to the source quartic;
- finite-`t` defect nonnegativity iff quartic nonnegativity;
- exact quartic coefficient expansion;
- equality of the leading coefficient with the separate `t=infinity` defect;
- exact positive and negative source regressions.

Still external/source-side:

- the analytic one-phase maximization used to eliminate one phase;
- identification of `(c0,a,b,c)` from genuine compact-F4 endpoints;
- uniform positivity of the resulting quartics over the genuine compact endpoint locus.

Therefore PR #18 is not a proof of compact-E6 simultaneous Weyl convexity.

## QI-D T48 — completed reduced-locus / zero-sector boundary result

The old handoff statement that T48 was unlaunched/open research is obsolete.

Canonical source:

- `QI-D/code/certify_E6_F4_endpoint_locus_reduction.py`;
- `QI-D/verification/certify_E6_F4_endpoint_locus_reduction.txt`;
- `QI-D/notes/T48_REPORT.md`.

T48 source certifies:

- the zero-restricted-root centralizer `m ≅ so(8)` with `dim m=28`, `dim a=2`;
- three positive A2 restricted roots of multiplicity `8`;
- triality multiplicity modules `8_v,8_s,8_c`;
- exact harmonic factorization `B_j=(1/2) Z_{L,j}^* Z_{R,j}` with `Z=[K,iP]`;
- PSD/rank outer endpoint-locus and Hilbert-Schmidt constraints;
- for all four certified E6 facets, exact Spin(8) zero-sector boundary values `Tr(F_aa)=1/3`, `support_W(D4)(F_tt)=2/3`, hence exact `1/3+2/3=1`, with active counts `24,20,16,14`.

The PSD/rank locus is only an outer relaxation. T48 does not prove interior compact-E6 Weyl convexity and does not produce a genuine compact counterexample.

## QI-D T49 — live research successor

`docs/agent_tasks/T49_qid_triality_endpoint_compatibility.md` now owns the unresolved exceptional interior.

T49 must derive exact common-F4/triality compatibility tying the three multiplicity-8 sectors and push those genuine-endpoint relations into the T33 quartic, or produce an exact genuine compact counterexample.

A point satisfying only T48's PSD/rank outer constraints is not a valid compact counterexample. Do not start a Lean “T49 proof” branch until source-side work produces a new exact theorem/certificate.

## Current next-work order

Re-audit live heads before every item. At this handoff the preferred order is:

1. **T48 four-facet zero-sector finite certificate.** Reuse the existing `FormalResearch.QID.D4DoublyStochasticCertificate` 192-element signed-permutation enumeration to formalize the exact `W(D4)` support `2/3`, active counts `24,20,16,14`, exact `Tr(F_aa)=1/3`, and combined `1/3+2/3=1` boundary certificate. Avoid rebuilding the full E6 root/triality representation unless later source work requires it.
2. **T48 adapted-basis / harmonic-block algebra**, if it remains a narrow exact theorem-sized target independent of the open interior compatibility theorem.
3. **QI-A T07 exact finite coding/guessing framework**, only if the operational hypotheses can be represented faithfully.
4. **General type-D support/Procrustes bridge**, if the necessary real-matrix/singular-value infrastructure is tractable enough to close the remaining semantic input.
5. Revisit exceptional-group interior Lean work only after T49 produces a new source-proved theorem/certificate.
6. Revisit QI-B/B4 only from a new source-proved result beyond T34's obstruction; do not formalize a refuted stronger route.

## Restart rules

1. Read this handoff, then current QIprojects `docs/lean_status.md`, `docs/agent_tasks/T20_REPORT.md`, and `docs/agent_tasks/T20_lean_small_targets.md`.
2. Recheck both repository heads before coding.
3. Read the theorem-local source note/script/log for the proposed target.
4. Start a fresh `agents/<task>-<short-name>` branch from current `formal-research-lean`.
5. Reject `sorry`/`admit`, hard-build the smallest changed module first, then import it into `FormalResearch.lean` and hard-build the full aggregate before promotion.
6. Record exact source pin, exact reasoner verification head, Actions run and external assumptions in the PR/update docs.
7. Merge only the exact tested code tree, or a source-identical cleanup tree whose Lean bytes are unchanged.
8. If Lean/source work materially exceeds current TeX, open a separate **draft/unmerged QIprojects review PR** before any manuscript integration.

## Claim discipline

- Keep SOURCE/PAPER, exact external certificate, FORMALIZED, MODULE-GREEN, AGGREGATE-GREEN, MERGED and full semantic theorem status distinct.
- Aggregate green proves compilation of the stated Lean theorem, not every analytic/operational/geometric premise surrounding it.
- Preserve source corrections and counterexamples; never restore a refuted claim to simplify formalization.
- Do not infer semantic `DS` membership from a finite certificate or exact pairing without the relevant bridge theorem.
- Do not turn T49 or any other open research task into a Lean completion target merely because it is strategically important.
- Do not turn PR #16's finite F4 Weyl-polytope certificate, PR #18's E6/F4 quartic reduction, or T48's outer-locus/boundary result into a compact exceptional-group theorem.

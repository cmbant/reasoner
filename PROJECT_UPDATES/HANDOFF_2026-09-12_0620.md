# Formal research handoff — 2026-09-12 06:20

This is the current restart authority for the QI-A Lean continuation. It supersedes the restart/status portions of `PROJECT_UPDATES/HANDOFF_2026-09-11_0740.md` and earlier handoffs. The earlier handoffs remain useful for provenance and PR history, but their live heads and frontier description are stale.

## Live authoritative heads

Research/manuscript authority:

- repository: `cmbant/QIprojects:main`
- live `main` head at this handoff: `a3960aa4542eeed8ae785fceac78aaf8c0319031`
- authoritative QI-A manuscript: `QI-A/paper/finite_copy_quantum_memory.tex`
- manuscript blob: `6f4af839cc7c13b0cb2b3704fcefdeb1d88e9195`
- the manuscript blob is unchanged from the September 11 continuation; recent QIprojects movement has been review/task-process work, not QI-A theorem text.

Lean authority:

- repository: `cmbant/reasoner:formal-research-lean`
- maintained branch head at this handoff: `2b1aacaef0c53adf709b08c19a9ecb1ca4207033`
- that head is merge commit for reasoner PR #55, `QI-A: assemble direct-sum isotypic auxiliary witnesses`.

Toolchain authority:

- pinned mathlib revision: `52b284ff128a523eca1f67c68d677861b95cc002`

Always recheck both live repository heads and the QI-A manuscript blob before implementation or merge. If the manuscript blob changes, re-audit the affected source theorem before continuing.

## Current QI-A Lean status

Do **not** report the QI-A main theorem as end-to-end Lean-certified. The downstream finite-dimensional converse is now strong and source-shaped, but the physical Schur/isotypic realization, CPTP semantics, coherent-width channel construction, and operational classicality bridge are still incomplete.

### Coherent-copy separation chain

Merged ordinary-proof-term modules now include:

- PR #46 `CoherentPowerSymmetricSpan`: diagonal coherent powers span the finite complex symmetric power through a complex-linear separation theorem;
- PR #47 `CoherentPhaseOrthogonality` + `HermitianCoherentPowerSeparation`: Hermitian coherent-copy separation on algebraic `SymmetricPower`;
- PR #48 `HilbertRealizationOperatorSeparation`: operator separation after supplying an explicit finite Euclidean realization `ι` of `SymmetricPower`;
- PR #49 `CoherentRankOneMatrixSeparation`: coherent rank-one Hermitian matrices span the full Hermitian matrix space in that realization.

Important scope boundary: pinned mathlib does not provide the physical Hilbert-space identification of algebraic `SymmetricPower` used by the manuscript. The realization `ι` is supplied explicitly. Do not invent or claim a canonical Hilbert instance that is not present.

### Deferred-query ordinary-memory converse chain

The earlier generic finite-dimensional chain is merged:

- pure-copy spanning / finite identification;
- Hermitian trace-pairing nondegeneracy;
- multiplicity-Hermitian product pairing;
- exact-recovery and no-recovery deferred-query memory lower bounds;
- statistics-on-spanning-family -> effect identity;
- canonical finite matrix trace adjoint;
- trace preservation iff the canonical trace adjoint is unital.

September 11–12 additions materially strengthen the semantic bridge:

- PR #50, merge `276bd0d9934b23d75073d8a4e22b312d0a659d39`: coherent rank-one source spanning composed with the canonical-adjoint deferred-query converse;
- PR #51, merge `2fc19ec33a3e789ef4066163e74bc803ece8e10c`: ordinary positivity transport for `matrixTraceAdjoint`; a PSD-preserving Schrödinger map has a PSD-preserving canonical trace adjoint. This is **ordinary positivity**, not complete positivity;
- PR #52, merge `cf2b4027af7ec7204c0dfc13cc2aa28ddef49b17`: positive trace-preserving encodings transport target POVMs through the canonical adjoint to source POVMs and normalize the pulled-back trace statistics;
- PR #53, merge `b61c86bbd28a0fef592de38507a05420d32db0bd`: PSD trace-one matrices satisfy `A ≤ I`, so normalized PSD witness states under a positive trace-preserving encoding automatically discharge the previous encoded-state order premise;
- PR #54, merge `ada4efed1ff566d725733410f18cba0b09d825bf`: manuscript auxiliary projectors/states are certified inside one finite isotypic product block;
- PR #55, merge `2b1aacaef0c53adf709b08c19a9ecb1ca4207033`: all auxiliary witnesses are assembled across varying isotypic sectors in one finite direct-sum matrix space and inserted into the positive trace-preserving coherent deferred-query converse.

### Exact PR #55 result

`FormalResearch/QIA/IsotypicDirectSumAuxiliaryStates.lean` models a source basis by a multiplicity-memory label `j = (a,r)` followed by a carrier coordinate `Fin (d a)`. It proves:

- the source query projector for each multiplicity basis label;
- the sharp global PVM identity;
- PSD, self-adjointness, and idempotence of the projectors;
- normalized PSD auxiliary states corresponding to `I_{V_a}/dim(V_a) ⊗ |e_{a,r}><e_{a,r}|`;
- unit trace and exact score `Tr(P_j omega_j)=1`.

`FormalResearch/QIA/DeferredQueryIsotypicChannelMemoryBound.lean` proves:

`multiplicityMemoryDimension g ≤ Fintype.card q`

from:

- sector dimensions `d`, multiplicities `g`, and `0 < d a`;
- an explicit linear equivalence
  `ι : SymmetricPower ℂ (Fin n) E ≃ₗ[ℂ] EuclideanSpace ℂ (isotypicDirectSumCarrier d g)`;
- continuity of coherent powers under `ι`;
- a positive trace-preserving encoding `C`;
- a target POVM `F`;
- equality of target-memory statistics with the explicit isotypic source-query statistics on coherent pure-copy inputs.

The caller no longer supplies separate source witness states, source projectors, witness positivity, trace-one, source-projector self-adjointness, diagonal-score proofs, pullback positivity, or encoded-state `≤ I` assumptions.

PR #55 validation:

- direct-sum witness focused-green head: `324a4d17c291f019177eeba5a5be3a4c08ab676f`, Actions run `34648926195`;
- two-module focused-green head: `224540800aeea49eaa67ed2d10b8520e76c3cea7`, Actions run `34649214328`;
- exact final tested candidate: `9869ff4f7096f998ffdedb97719208bafd9723b3`, Actions run `34649563328`, which passed placeholder rejection, both focused modules, and full `lake build FormalResearch`;
- cleanup head: `cd73a0aa80870b94cec7ee77f9e1b051676cf434`; the cleanup diff removed only the temporary workflow;
- merge commit: `2b1aacaef0c53adf709b08c19a9ecb1ca4207033`.

## What remains load-bearing

The current finite matrix theorem is close to the manuscript's lower-bound argument **once the physical source is identified with the isotypic carrier**. The missing pieces are now concentrated upstream and in the other memory notions.

1. **Physical symmetric-copy / Schur-isotypic realization.** Construct or minimally axiomatize, in a source-faithful way, the finite-dimensional physical realization that justifies the current `ι : SymmetricPower -> isotypicDirectSumCarrier d g`, including the relation to the manuscript's symmetric-copy Hilbert space, sector decomposition, source projectors, and reduced states. Do not confuse the existing algebraic `SymmetricPower` results with this physical identification.
2. **Concrete symmetry-reduction / commutant / multiplicity-algebra channel.** Formalize the finite channel/reduction used by the manuscript, rather than only the already-certified block bookkeeping. A useful first step may be a trace-statistics-preserving unitary/isometric transport into the direct-sum carrier if that is genuinely needed by the source realization; avoid wrappers with no manuscript role.
3. **Complete positivity / CPTP semantics.** PRs #51–#55 use ordinary positivity + trace preservation. This is enough for the current finite lower-bound theorem but is weaker than physical CPTP certification. Inspect pinned mathlib's completely-positive-map APIs before introducing custom abstractions. Do not rename ordinary positivity results as CP/CPTP.
4. **Coherent-width physical bridge.** The finite factorization lower-bound core and carrier bookkeeping are already merged, but the physical CPTP/Schmidt-number interpretation and explicit classically flagged upper construction remain open.
5. **Operational classical-memory iff.** The algebraic block-commutativity / multiplicity-free criterion is merged, but the operational classical channel / entanglement-breaking / joint-measurability bridge remains open.
6. **Other theorem-local trust boundaries.** Preserve the prior T39/T07/QI-D distinctions. In particular, the T39 finite permutation table uses `native_decide`, and the analytic Haar identity is still external.

## Recommended next-work order

1. Recheck `QIprojects:main`, `reasoner:formal-research-lean`, and the QI-A manuscript blob.
2. Read the source theorem around the Schur/isotypic reduction and compare it directly with `DeferredQueryIsotypicChannelMemoryBound.lean`.
3. Inspect pinned mathlib for finite-dimensional unitary/isometry, tensor-power, symmetric-subspace, representation/isotypic, and completely-positive-map APIs before designing new structures.
4. Take the **smallest source-faithful physical realization bridge** that makes one current external premise of `DeferredQueryIsotypicChannelMemoryBound` into a proved fact. Prefer a real theorem over a convenience wrapper.
5. Only after that, extend toward the concrete CPTP symmetry-reduction channel. If a clean CP abstraction is unavailable, keep ordinary-positive and physical-CPTP layers explicitly separate.
6. Then tackle coherent-width CPTP/Schmidt semantics and the flagged upper channel.
7. Leave the operational classical-memory iff until the physical channel model is available unless a genuinely tiny, source-useful lemma appears.

## Merge discipline

Continue the established discipline for every Lean PR:

1. Branch from the current maintained `formal-research-lean` head.
2. Keep the target small and source-faithful.
3. No `sorry` / `admit`.
4. Use a temporary branch-only workflow when needed.
5. Hard-build the smallest changed module(s) first.
6. Freeze theorem source after focused green.
7. Import the module(s) from `FormalResearch.lean` and hard-build full `FormalResearch` on the exact promoted tree.
8. Record exact tested candidate SHA and Actions run.
9. Delete only the temporary workflow after green.
10. Compare tested candidate to cleanup head and require no Lean/import changes.
11. Recheck `QIprojects:main` and the manuscript blob immediately before merge.
12. Mark ready and merge with `expected_head_sha` equal to the verified cleanup head.
13. Verify `formal-research-lean` moved to the merge commit.

Do not merge on a focused build alone. Do not overclaim end-to-end QI-A certification.

## Working style for the next agent

The user expects direct repository work, not analysis-only discussion. Continue autonomously through small PRs and CI/merge validation. Give concise progress updates during long tool sequences. Do not ask what to do next unless genuinely blocked by missing source information or permissions. If a proof fails, fetch the exact Lean diagnostic and repair only the concrete issue rather than weakening the theorem.

## Restart authority

Read this file first. The previous `HANDOFF_2026-09-11_0740.md` remains useful for older PR history, but this file controls live pins and the post-PR-55 frontier.

# Lean 4.34.0 post-merge status — 2026-09-20

## Canonical repositories

- Research/manuscript authority: `cmbant/QIprojects:main`.
- Lean authority: `cmbant/reasoner:master`.
- The former `formal-research-lean` branch was squash-merged into `master`.
- The resulting merge commit is recorded by `git rev-parse master` after the remote update.

## Toolchain and dependency pins

- Lean: `leanprover/lean4:v4.34.0` from `lean-toolchain`.
- mathlib: revision `5ed2965256430c3649e86755f9576b54eca72435` in `lakefile.toml`.
- The mathlib revision uses the same stable Lean 4.34.0 toolchain.

## Local reproduction

The clean checkout workflow was reproduced locally with:

```text
lake update
lake exe cache get
placeholder scan for sorry/admit
lake build
```

All steps passed. The aggregate build completed successfully with 9,130 jobs. The cache command reported two unavailable cache files but exited successfully after restoring the available cache entries; the subsequent aggregate build passed.

## Branch policy

The default branch is now the single maintained Lean integration branch. New work should branch from `master` and target `master`. Historical handoffs retain their original `formal-research-lean` references as provenance, but they are not current branch instructions.

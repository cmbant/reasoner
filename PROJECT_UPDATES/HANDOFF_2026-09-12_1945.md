# Formal research handoff — 2026-09-12 19:45

This is the current restart authority for the QI-A Lean continuation. It supersedes the restart/status portions of `PROJECT_UPDATES/HANDOFF_2026-09-12_0620.md` and earlier handoffs. Earlier handoffs remain useful for provenance and older PR history, but their live heads and frontier descriptions are stale.

This handoff includes work completed after the 19:45 continuation request, through reasoner PR #66.

## Live authoritative heads

Research/manuscript authority:

- repository: `cmbant/QIprojects:main`
- live `main` head at the final pre-PR-66 merge check: `805e7c688b0b79b9f636ab64e6e27e0bb018a8a4`
- authoritative QI-A manuscript: `QI-A/paper/finite_copy_quantum_memory.tex`
- manuscript blob: `6f4af839cc7c13b0cb2b3704fcefdeb1d88e9195`
- the manuscript blob remained unchanged throughout PRs #56–#66; recent QIprojects head movement has not changed this theorem source.

Lean authority:

- repository/branch: `cmbant/reasoner:formal-research-lean`
- maintained branch head at this handoff: `5c49d2750c0a570e10fffa449a5eda50cd0f6849`
- that head is the merge commit for reasoner PR #66, `QI-A: assemble global isotypic base-field decomposition`.

Toolchain authority:

- pinned mathlib revision: `52b284ff128a523eca1f67c68d677861b95cc002`

Always recheck `QIprojects:main`, the QI-A manuscript blob, and `reasoner:formal-research-lean` before implementation and immediately before merge. If the manuscript blob changes, re-audit the affected source theorem before continuing.

## Current QI-A Lean status

Do **not** report the QI-A main theorem as end-to-end Lean-certified.

The finite lower-bound/converse side is now strong, the explicit isotypic multiplicity reduction is genuinely completely positive in Mathlib's `CompletelyPositiveMap` API, and Mathlib's abstract semisimple-module isotypic decomposition has been connected to base-field linear coordinates. The main missing load-bearing step is still the physical symmetric-copy representation / Schur--Weyl realization that connects the manuscript's physical Hilbert space to this algebraic finite carrier model.

### Previously merged lower-bound and witness chain

The earlier chain remains authoritative:

- coherent powers span the algebraic finite symmetric power;
- Hermitian coherent-copy separation;
- explicit finite Euclidean realization for operator separation;
- coherent rank-one Hermitian matrices span the full Hermitian matrix space;
- canonical trace adjoint and its ordinary positivity/unital/POVM transport properties;
- PSD trace-one matrices satisfy `A ≤ I`;
- explicit one-block and direct-sum isotypic auxiliary projectors/states;
- deferred-query lower bound on `multiplicityMemoryDimension g` once an explicit physical/source coordinate equivalence and the relevant channel/statistics hypotheses are supplied.

Preserve the earlier trust boundary: algebraic `SymmetricPower` does not come with the physical Hilbert-space identification needed by the manuscript. Do not invent a canonical Hilbert structure or claim that the existing finite Euclidean realization is the physical Schur transform.

## PRs #56–#66: symmetry reduction, CP, and algebraic coordinates

### PR #56 — finite isotypic multiplicity reduction

Merge commit: `8c37c067f6fd612c3f40c3d3145530d08761b8da`

File: `FormalResearch/QIA/IsotypicMultiplicityReduction.lean`.

This introduced the explicit finite block reduction matching the manuscript's multiplicity-state bookkeeping after isotypic coordinates have already been supplied, including the trace identity and source-query statistics identity. It does not construct the physical Schur/isotypic coordinates.

### PR #57 — sector partial-trace ordinary positivity

Merge commit: `9d2ce8eed271c891befcad51415cb42f0a46ec85`

File: `FormalResearch/QIA/IsotypicMultiplicityReductionPositivity.lean`.

Proves ordinary PSD preservation for a single sector block. This is ordinary positivity, not complete positivity.

### PR #58 — assembled ordinary positivity

Merge commit: `239389be77610edac92909d9ca7604c4484d28df`

File: `FormalResearch/QIA/IsotypicMultiplicityReductionPositive.lean`.

Assembles sector positivity through the direct-sum reduction. Still ordinary positivity only.

### PR #59 — sector finite-ancilla positivity

Merge commit: `81f9a045941fc4bd1994620fb5996f2da0f74517`

File: `FormalResearch/QIA/IsotypicMultiplicityBlockAmplification.lean`.

Shows raw matrix PSD preservation for a sector block after arbitrary finite ancilla amplification.

### PR #60 — assembled finite-ancilla positivity

Merge commit: `e7d52651a3a571b1495414eeba12f6e1fd38f143`

File: `FormalResearch/QIA/IsotypicMultiplicityReductionAmplification.lean`.

Key theorem: `isotypicMultiplicityReduction_amplification_posSemidef`.

For arbitrary finite ancilla `β`, the full assembled reduction preserves raw `Matrix.PosSemidef` on the ancilla-first product index. This is the raw amplification theorem later used to prove genuine complete positivity.

### PR #61 — `CStarMatrix` spectral nonnegativity ↔ raw PSD

Merge commit: `12fb4fc88e97d02c4430fbc68f9805b51f3302eb`

File: `FormalResearch/QIA/CStarMatrixPosSemidefOrder.lean`.

Key theorem: `cstarMatrix_ofMatrix_nonneg_iff_posSemidef`.

The proof identifies the spectral nonnegative cone and raw matrix PSD cone through `StarOrderedRing.nonneg_iff`. This is an order-transport theorem, not itself a CP statement.

### PR #62 — nested `CStarMatrix` block flattening

Merge commit: `a648c69097ebc11a33af638497e7299d29caba9b`

File: `FormalResearch/QIA/CStarMatrixBlockFlattenPositivity.lean`.

Uses Mathlib's canonical `Matrix.comp` to flatten nested finite matrix algebras and proves nested spectral nonnegativity implies flattened raw PSD.

### PR #63 — bidirectional block-flatten positivity bridge

Merge commit: `b6e8528c7d82f3d3394d62c384c194668da26140`

The nested bridge was strengthened to the exact equivalence

`0 ≤ M ↔ (cstarMatrixBlockFlatten M).PosSemidef`.

The reverse direction transports star-square-generated nonnegative cones through canonical unflattening. This supplies the output-side bridge needed by the CP constructor.

### PR #64 — genuine completely positive isotypic reduction

Merge commit: `320c992171993da5a366c59394c358ffe9a18748`

File: `FormalResearch/QIA/IsotypicMultiplicityReductionCompletelyPositive.lean`.

This is the first genuine Mathlib `CompletelyPositiveMap` result for the explicit finite isotypic multiplicity reduction. The proof:

1. transports the raw matrix reduction through `CStarMatrix.ofMatrixₗ`;
2. converts nested spectral nonnegativity to flattened raw PSD;
3. applies PR #60's arbitrary finite-ancilla PSD theorem;
4. identifies `CStarMatrix.map` with the raw amplified formula entrywise;
5. converts the raw output PSD back to nested spectral nonnegativity.

Validation:

- frozen theorem source: `cf6244d0c78c5e2643e6a1940af2aeae151ae3bb`
- focused run: `34703407488`, job `103579030690`, success
- exact promoted candidate: `e62ba62d8f92c64f23b24dd6cc0968ca27564341`
- promoted run: `34703600209`, job `103579578858`, success including full `lake build FormalResearch`
- cleanup head: `13613f391ee600d09b392c660ab541065f4d31cb`
- merge commit: `320c992171993da5a366c59394c358ffe9a18748`

Trace preservation remains separately proved by `isotypicMultiplicityReduction_trace`. Pinned Mathlib did not expose a native `QuantumChannel`/CPTP bundle during the audit. Do not invent a wrapper solely to rename the separate CP + trace-preservation facts as a native CPTP object.

### PR #65 — finite isotypic block coordinates over the physical base field

Merge commit: `858306b3fd250b06995203acb40bef9e892b097e`

File: `FormalResearch/QIA/IsotypicBaseFieldCoordinates.lean`.

Key theorem: `isotypicBaseFieldMultiplicityCoordinates`.

Mathlib's `IsIsotypicOfType.linearEquiv_fun` gives, for a finite semisimple `A`-module block `M` of simple type `S`, an `A`-linear equivalence

`M ≃ₗ[A] (Fin g → S)`

for some finite multiplicity `g`. PR #65 restricts scalars along `F → A` and obtains the genuinely base-field-linear coordinate equivalence

`M ≃ₗ[F] (Fin g → S)`.

Validation:

- frozen theorem source: `016b1d60f8e9d98a36849a1bd6a7ada6b98dd42f`
- focused run: `34709371587`, job `103595175384`, success
- exact promoted candidate: `83d9bd4d9061443dc0fec3996d5cbb0a9a650757`
- promoted run: `34709691759`, job `103596046692`, success including full aggregate
- cleanup head: `5f0f6777d7665dcfbef763cdd624b8c4572b4473`
- merge commit: `858306b3fd250b06995203acb40bef9e892b097e`

Scope boundary: this proves algebraic coordinate existence for one finite isotypic block. It does not prove compact-unitary Schur--Weyl decomposition or unitary/isometric coordinates.

### PR #66 — global semisimple isotypic decomposition over the base field

Merge commit: `5c49d2750c0a570e10fffa449a5eda50cd0f6849`

File: `FormalResearch/QIA/GlobalIsotypicBaseFieldDecomposition.lean`.

Key theorem: `globalIsotypicBaseFieldDecomposition`.

For a semisimple `A`-module `M` with a compatible scalar tower `F → A → M`, it proves

`Nonempty (M ≃ₗ[F] (Π₀ c : isotypicComponents A M, c.1))`.

The proof uses Mathlib's actual semisimple/isotypic infrastructure:

- `sSupIndep_isotypicComponents`;
- `sSupIndep_iff` to convert set independence to indexed independence;
- `sSup_isotypicComponents` and `sSup_eq_iSup'` for spanning;
- `iSupIndep.linearEquiv` for the global algebra-linear direct-sum decomposition;
- `LinearEquiv.restrictScalars` for the physical base-field linear equivalence.

One initial focused attempt failed only on a brittle spanning rewrite. The repaired proof uses the exact `sSup_eq_iSup'` identity; the theorem statement was not weakened.

Validation:

- failed initial source: `49f82fb0e6e71fa5586fff95a48d229f623d79e3`
- failed run: `34712248281`, job `103602993142`
- frozen focused-green theorem source: `a83e946e40382a6098f344fe28a04fbe3782da8d`
- focused run: `34712372767`, job `103603332305`, success
- exact promoted candidate: `798966451daf3482715edd6d6fd49f4e76b3c571`
- promoted run: `34712509296`, job `103603707115`, success: placeholders, focused module, and full `lake build FormalResearch`
- cleanup head: `f10b76016151e6fd0dd504f8adc4d2a8d84e6770`
- promoted → cleanup changed only the temporary workflow
- merge commit: `5c49d2750c0a570e10fffa449a5eda50cd0f6849`

Scope boundary: PR #66 gives the global **algebraic** isotypic direct sum over the base field. It does not prove that the manuscript's physical symmetric-copy Hilbert space is this semisimple representation module, nor that the equivalence is unitary/isometric.

## Pinned Mathlib APIs now known to be useful

At revision `52b284ff128a523eca1f67c68d677861b95cc002`:

- `Mathlib/RingTheory/SimpleModule/Isotypic.lean`
  - `IsIsotypicOfType.linearEquiv_fun`
  - `IsIsotypic.isotypicComponents`
  - `sSupIndep_isotypicComponents`
  - `sSup_isotypicComponents`
  - finite isotypic-component machinery under Noetherian/finite hypotheses
- `Mathlib/LinearAlgebra/DFinsupp.lean`
  - `iSupIndep.linearEquiv`
  - `DFinsupp.mapRange.linearEquiv`
- `Mathlib/LinearAlgebra/DirectSum/Finite.lean`
  - `DFinsupp.linearEquivFunOnFintype`
  - finite direct-sum / `Module.Finite` infrastructure
- `Mathlib/LinearAlgebra/Dimension/Free.lean`
  - `Module.finBasis`, with `Fin (finrank F V)` coordinates for finite free modules
- `Mathlib/Analysis/CStarAlgebra/CompletelyPositiveMap.lean`
  - `CompletelyPositiveMap` / scoped `→CP`
  - matrix-amplification positivity is the CP criterion
- `Mathlib/Data/Matrix/Composition.lean`
  - `Matrix.comp`, `compRingEquiv`, `compAlgEquiv`, the canonical nested-matrix flattening machinery used by PRs #62–#64.

## What remains load-bearing

### 1. Explicit finite carrier assembly from the algebraic isotypic decomposition

This is now the smallest useful algebraic bridge before the genuinely physical representation theorem.

PR #66 gives

`M ≃ₗ[F] Π₀ c : isotypicComponents A M, c.1`.

PR #65 gives finite multiplicity coordinates inside one finite isotypic block. The existing QI-A channel/witness model expects the explicit scalar carrier

`isotypicDirectSumCarrier d g = Σ j : multiplicityMemoryCarrier g, Fin (d j.1)`.

A useful next theorem should, under source-faithful finiteness assumptions, combine:

1. finite indexing of the nonzero isotypic components;
2. a finite multiplicity coordinate model for each component;
3. a finite `F`-basis for each chosen simple factor;
4. `DFinsupp.linearEquivFunOnFintype` / finite direct-sum reindexing;
5. Sigma reindexing to the existing `isotypicDirectSumCarrier d g`.

The target should be a genuine base-field linear equivalence into an explicit finite scalar coordinate space close to

`M ≃ₗ[F] (isotypicDirectSumCarrier d g → F)`.

Avoid stopping at another convenience wrapper around an assumed equivalence. The point is to **derive** as much of the current `ι`-style carrier premise as Mathlib's semisimple/isotypic theory genuinely supports.

Be careful about hypotheses. `isotypicComponents A M` is finite under suitable Noetherian assumptions; in the physical finite-dimensional setting it should be possible to state a clean finite theorem, but do not silently assume finiteness of the representation algebra/module if it has not been established.

### 2. Physical symmetric-copy / Schur--Weyl realization

This remains the central external premise.

The manuscript uses a physical decomposition

`H ≅ ⊕_α V_α ⊗ M_α`

for the relevant compact local-unitary representation, with sector projectors `Π_α` and reduced multiplicity states

`σ_α(ρ) = Tr_{V_α}(Π_α ρ Π_α)`.

Current Lean results do **not** yet prove:

- that the physical symmetric-copy space carries the needed semisimple representation module structure;
- the compact-unitary Schur--Weyl/isotypic decomposition for that representation;
- that algebraic `SymmetricPower` is the manuscript's physical Hilbert realization;
- that the resulting coordinate equivalence is unitary/isometric;
- that the manuscript's physical projectors/partial traces are transported to the explicit finite matrix reduction.

This should remain the main research target after the explicit finite carrier assembly, unless a smaller theorem directly proves one of these bullets.

### 3. Physical channel / reduced-state identification

The explicit finite reduction is now genuinely CP and separately trace preserving. What remains is to prove that, under the physical Schur/isotypic coordinate realization, it is the manuscript's symmetry-sufficient reduction and that the `eq:liftprob` trace-statistics identity is the transported physical statement.

Do not claim this from CP alone.

### 4. CPTP packaging

Complete positivity and trace preservation are now both available as separate facts for the explicit finite reduction. No native pinned-Mathlib `QuantumChannel`/CPTP bundle was found in the audit. Native packaging is therefore lower priority than the physical realization. Do not introduce a custom wrapper unless it closes a concrete manuscript dependency.

### 5. Coherent-width physical bridge

The finite factorization lower-bound core and carrier bookkeeping are merged, but the physical CPTP/Schmidt-number interpretation and explicit classically flagged upper construction remain open.

### 6. Operational classical-memory iff

The algebraic multiplicity-free/block-commutativity criterion is merged, but the operational classical channel / entanglement-breaking / joint-measurability bridge remains open. Defer this until the physical channel model is in place unless a genuinely tiny source-useful theorem appears.

### 7. Other trust boundaries

Preserve earlier distinctions outside this frontier. In particular:

- T39's finite permutation table uses `native_decide`;
- the analytic Haar identity remains external where previously documented.

## Recommended next-work order

1. Recheck `QIprojects:main`, the QI-A manuscript blob, and `reasoner:formal-research-lean`.
2. Read `GlobalIsotypicBaseFieldDecomposition.lean`, `IsotypicBaseFieldCoordinates.lean`, `IsotypicDirectSumAuxiliaryStates.lean`, and `DeferredQueryIsotypicChannelMemoryBound.lean` together.
3. Take the smallest theorem that assembles the abstract global isotypic DFinsupp coordinates and per-block finite coordinates into the existing explicit finite `isotypicDirectSumCarrier d g` scalar carrier. Use real finiteness hypotheses and Mathlib's finite-direct-sum equivalences; avoid merely restating an equivalence as an input.
4. Once that algebraic carrier assembly is proved, attack the true physical premise: equip/identify the symmetric-copy representation with the appropriate finite semisimple representation module and derive the relevant isotypic decomposition.
5. Only after the physical coordinate map is available, prove compatibility with the physical Hilbert structure/projectors/partial trace and transport the already-certified CP + trace-preserving finite reduction back to the manuscript's channel.
6. Then tackle coherent-width CPTP/Schmidt semantics and the flagged upper construction.
7. Leave the operational classical-memory iff until the physical channel model is available unless a small theorem directly advances it.

## Merge discipline

Continue the established discipline for every Lean PR:

1. Branch from the current maintained `formal-research-lean` head.
2. Keep the target small and source-faithful.
3. No `sorry` / `admit`.
4. Use a temporary branch-only workflow when needed.
5. Hard-build the smallest changed module(s) first.
6. Freeze theorem source after focused green.
7. Import the module(s) from `FormalResearch.lean` and hard-build full `FormalResearch` on the exact promoted tree.
8. Record exact tested candidate SHA and Actions run/job.
9. Delete only the temporary workflow after green.
10. Compare tested candidate to cleanup head and require no Lean/import byte changes.
11. Recheck `QIprojects:main`, the manuscript blob, and maintained reasoner head immediately before merge.
12. Mark ready and merge with `expected_head_sha` equal to the verified cleanup head.
13. Verify `formal-research-lean` moved to the merge commit.

Do not merge on a focused build alone. If CI fails, fetch the exact Lean diagnostic and repair only the concrete proof/API issue rather than weakening the theorem.

## Working style for the next agent

The user expects direct repository work, not analysis-only discussion. Continue autonomously through small PRs and CI/merge validation. Give concise progress updates during long tool sequences. Do not ask what to do next unless genuinely blocked by missing source information or permissions.

Preserve semantic distinctions:

- ordinary positivity ≠ complete positivity;
- CP + a separate trace theorem is not automatically a native bundled CPTP object;
- algebraic semisimple/isotypic coordinates ≠ physical compact-unitary Schur--Weyl coordinates;
- a linear equivalence ≠ a unitary/isometric equivalence;
- algebraic `SymmetricPower` ≠ a proved physical symmetric-copy Hilbert realization.

## Restart authority

Read this file first.

The previous `PROJECT_UPDATES/HANDOFF_2026-09-12_0620.md` remains useful for older provenance but no longer controls the live heads or frontier. This file is the restart authority after PR #66.

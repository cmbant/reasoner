# Formal research handoff — 2026-09-11

This supersedes the restart/status portions of `HANDOFF_2026-09-10_1400.md` and earlier handoffs.

## Current authoritative heads

Research/manuscript/certificate authority:

- repository: `cmbant/QIprojects:main`
- current main head at this handoff: `f96b5b0be457867f068fc40461853048e346239d`
- authoritative QI-A manuscript: `QI-A/paper/finite_copy_quantum_memory.tex`

Lean authority:

- repository: `cmbant/reasoner:formal-research-lean`
- maintained branch head before this documentation-only handoff PR: `bbbc7b745cdacca2ceca5978711fae9d085fd602`
- latest merged Lean-code head: `bbbc7b745cdacca2ceca5978711fae9d085fd602`

Always recheck both live heads before new implementation.

## What changed since the 2026-09-10 14:00 handoff

The old handoff's first two QI-A targets are closed:

- reasoner PR #26 formalized the T39 finite operational-fingerprint dimensions (`K=14`, `R=20`, normalized coordinate bookkeeping `19`, odd dimension `3`);
- reasoner PR #27 formalized the finite `S_6`/symbolic sparse-witness second-moment certificate. Its 720-permutation table uses `native_decide`; the symbolic reduction is ordinary Lean. The analytic Haar/symmetric-subspace expectation identity remains external.

The aggregate-coverage orphan called out in QIprojects `docs/lean_status.md` was also fixed:

- reasoner PR #28 imported `FormalResearch.Bridges.B2BlaschkeMultiplicityRank` into `FormalResearch.lean` and built it in the aggregate. The theorem bytes were pre-existing and use ordinary proof terms.
- Therefore the old `155 of 161` aggregate-reachability paragraph is stale. Many modules were added after that count. Do not quote a replacement ratio until a fresh module/reachability audit is performed.

Most importantly, PRs #29–#43 built a substantial finite-dimensional semantic layer for the main QI-A memory theorems.

## QI-A main-theorem Lean frontier

### Downstream pure-copy spanning / reversible-memory core

Merged ordinary-proof-term modules now cover:

- PR #29 `PureCopyMemoryCore`: finite-dimensional separation -> spanning, fixing a spanning family -> identity, perfect finite identification -> memory cardinality bound;
- PR #33 `MatrixTracePairing`: nondegenerate full complex matrix trace pairing and duality;
- PR #34 `HermitianInnerPairing`: real Hermitian trace-pairing nondegeneracy and span from trace separation;
- PR #35 `MultiplicityHermitianTracePairing`: product/direct-sum Hermitian multiplicity-algebra pairing, span from trace separation, and recovery-extension identity;
- PR #36 `ExactRecoveryHeisenbergMemoryBound`: exact-recovery dimension lower bound from a positive unital trace-adjoint pullback;
- PR #40 `MatrixTraceAdjoint`: canonical finite trace adjoint and its defining/uniqueness theorem;
- PR #41 `DeferredQueryCanonicalAdjointMemoryBound`: no-recovery deferred-query dimension converse using the canonical trace adjoint rather than an arbitrary adjoint witness;
- PR #42 `MatrixTraceAdjointUnital`: trace preservation iff the canonical trace adjoint is unital; merged after run `34557340900` passed isolated and full aggregate builds.

These steps remove most of the generic finite-dimensional linear/Heisenberg bookkeeping downstream of source-family spanning.

### No-recovery deferred-query chain

Merged ordinary-proof-term modules now cover:

- PR #37 `DeferredQueryPostSeparationMemoryBound`: operator identity -> perfect memory identification -> dimension bound, with no recovery map;
- PR #38 `SpanningStatisticsEffectIdentity`: equality of query statistics on a spanning Hermitian family -> equality of Hermitian effects;
- PR #39 `DeferredQuerySpanningMemoryBound`: direct composition, so spanning query statistics force the memory-dimension lower bound;
- PR #41 replaces the arbitrary adjoint witness in that direct theorem by `matrixTraceAdjoint C`.

Thus the remaining load-bearing source hypothesis in this chain is no longer generic finite linear algebra: it is the actual coherent-power separation/spanning theorem plus the physical symmetry/channel identification and positivity facts.

### Upper bounds, coherent width, and classicality

Already merged:

- PR #30 `CoherentWidthFactorization`: matrix-rank factorization lower-bound core for `w_min = max_alpha dim M_alpha`;
- PR #31 `MultiplicityBlockClassicality`: full matrix blocks commute iff every multiplicity is at most one, with explicit noncommuting matrix-unit witnesses otherwise;
- PR #32 `CanonicalMemoryCarriers`: direct-sum carrier cardinality `K_G`, common coherent carrier width `max g`, and explicit flagged embeddings.

Scope boundaries remain important:

- the coherent-width theorem still lacks the physical CPTP/Schmidt-number -> finite matrix factorization bridge and the explicit classically flagged channel construction;
- the classicalization iff theorem still lacks the operational classical-channel / entanglement-breaking or joint-measurability bridge;
- carrier cardinalities are not themselves a construction of the Schur/isotypic CPTP reduction.

### Polarization support

`FormalResearch.QIA.SymmetricMultilinearDiagonal` is merged in PR #43. It passed isolated compilation and the full aggregate in run `34557425952` on a tree already containing PR #42.

It proves: a symmetric continuous real `n`-linear map that vanishes on its diagonal is zero, using mathlib's iterated-derivative diagonal formula.

This is useful polarization infrastructure, but it is **not** the paper's coherent-copy separation lemma by itself. The source-specific missing bridge is still to construct/identify the relevant Hermitian bidegree `(d,d)` multilinear form from the pure-copy quadratic statistic, prove the required symmetry/injectivity, and connect it to the actual symmetric-power/isotypic Hilbert model. Mathlib's present algebraic `SymmetricPower` API does not make that bridge automatic.

## Main-result coverage assessment

Do not report a single percentage as a formal metric.

A reasonable engineering estimate after the above merges is:

- roughly **50–60% of the headline QI-A proof infrastructure/ingredients** is now Lean-certified;
- **0% of the full main theorem end-to-end** is represented as one source-faithful theorem from the physical pure-copy model through symmetry reduction to the final optimal-memory/classicality conclusions.

What is genuinely strong now is the downstream finite algebra/linear/Heisenberg argument once the source-specific separation and channel models are supplied.

The hardest central missing pieces are:

1. coherent-copy separation / pure-power spanning in the actual `Sym^d(V)` setting;
2. concrete symmetry reduction / commutant / multiplicity-algebra identification and the CPTP channel realizing it;
3. positivity/complete-positivity semantic transport for canonical adjoints where a physical theorem needs it;
4. the coherent-width CPTP/Schmidt semantic bridge and explicit upper channel;
5. the operational classical-memory iff direction.

## Trust boundaries

Keep theorem-local trust distinctions.

- The new semantic QI-A modules PRs #29–#43 use ordinary proof terms unless explicitly noted above.
- T39 `SparseWitnessHaarSecondMoment` uses `native_decide` for the finite 720-permutation table; do not flatten that into kernel reduction.
- The analytic Haar identity is still external.
- Aggregate-green means only modules reachable from `FormalResearch.lean`; the historical `155/161` count is stale after PR #28 and the later module additions.

## Source status

QIprojects `main` remained `f96b5b0be457867f068fc40461853048e346239d` throughout this continuation.

The QI-A manuscript theorem order and scope relevant here are unchanged:

- coherent-copy separation is the source-specific input to pure-copy spanning;
- pure-copy spanning gives reversible-memory rigidity and ordinary-memory lower bound `K_G`;
- free classical labels give coherent width `max_alpha dim M_alpha`;
- no-recovery exact deferred-query memory still has lower bound `K_G`;
- exact classicalization is tied to multiplicity-freeness/commutativity.

No manuscript TeX correction was required in this continuation.

## Recommended next-work order

1. **Coherent-power separation bridge.** Use the new diagonal-polarization helper if useful, but do not confuse generic symmetric multilinear polarization with the missing Hermitian bidegree/symmetric-power bridge. The first concrete target should isolate a source-faithful scalar separation theorem on finite-dimensional `V` before attempting the full representation decomposition.
2. **Concrete symmetry-reduction/channel model.** Formalize the finite isotypic/multiplicity map needed to instantiate the abstract Hermitian multiplicity algebra and its physical states/effects.
3. **Canonical-adjoint positivity bridge**, only once needed by a source-faithful physical statement. Trace-adjoint existence and unitality from trace preservation are now algebraic support; positivity is the genuinely nontrivial remaining channel-side fact.
4. **Exact-recovery canonical-adjoint wrapper** if it materially simplifies the paper theorem after item 2; do not prioritize wrappers over the coherent-power bridge.
5. **Coherent-width Schmidt/CPTP bridge** and explicit flagged upper channel.
6. **Operational classicalization iff** after the physical channel model exists.
7. Re-audit other projects theorem-locally; do not let the QI-A push weaken QI-D/T07/T39 trust/scope boundaries.

## Restart and merge discipline

1. Read this handoff first.
2. Recheck QIprojects `main` and reasoner `formal-research-lean`.
3. Re-audit the theorem-local source if QIprojects moved.
4. Start each Lean target from maintained reasoner head.
5. Reject `sorry` / `admit`.
6. Hard-build the smallest changed module.
7. Import from `FormalResearch.lean` and hard-build full `FormalResearch` on the exact promoted tree.
8. Record source pin/blob, module blob, aggregate blob, Actions run and external hypotheses.
9. Merge only the exact tested tree or cleanup tree whose Lean source/import bytes are identical to the tested tree.
10. Preserve the distinction among source theorem, external certificate, Lean algebra, module-green, aggregate-green, merged and full semantic theorem.

## QIprojects status integration

A separate QIprojects documentation/status PR is appropriate for this continuation because the old `docs/lean_status.md` aggregate-orphan statement is now stale and the QI-A semantic frontier changed materially. The status update should be non-destructive: explicitly supersede the stale aggregate-count/B2 paragraph rather than pretending it was never true, and point back to this handoff as the restart authority.

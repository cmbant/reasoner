# Formal research handoff — 2026-09-07

This is the current restart point for `cmbant/reasoner:formal-research-lean` after the September 8–9 Lean aggregate-maintenance pass, the QI-D finite-facet repair, and a fresh reconciliation against live `cmbant/QIprojects:main`.

It supersedes the earlier contents of this file. Keep `PROJECT_UPDATES/HANDOFF_2026-09-06.md` as historical context, especially for old Endpoint14 dead ends, but do not use its repository pins as current state.

## Current status at a glance

The maintained Lean code tree is now **AGGREGATE-GREEN**.

Exact verified code head:

`3e27e2ed6e0ea0671335c428fe80178b9c1f6639`

Exact verified tree:

`53ade79e523314cfa16d59c23555e1c6542821ea`

On that exact head/tree, GitHub Actions run `34313366768` passed:

- placeholder rejection;
- `FormalResearch.QIC.Endpoint14ReducedPolynomialIdentity`;
- the full Endpoint14 determinant/Gaussian bridge;
- `lake build FormalResearch`.

The same head also passed the complete failure-only frontier in run `34313366655`.

A documentation-only commit updating this handoff follows the verified code head. Claims of `AGGREGATE-GREEN` below refer to the exact verified code tree above, not merely to a moving branch name.

## Exact repository heads and source authority

At the final source recheck:

- research/source authority: `cmbant/QIprojects:main`
  - `98930c46ed1ddb27237e6e8d37c3b607ad956796`
- exact Lean code verification marker: `cmbant/reasoner:formal-research-lean`
  - `3e27e2ed6e0ea0671335c428fe80178b9c1f6639`
  - tree `53ade79e523314cfa16d59c23555e1c6542821ea`
- merge commit that promoted the QI-D repairs:
  - `8ef0b96dc41127ac2712d686d6728703259fd7c8`
  - same tree `53ade79e523314cfa16d59c23555e1c6542821ea`
- green committed-source QI-D validation head:
  - `34cc436b3f31d29123ff1d7228018ea3807cfd5e`
  - same tree `53ade79e523314cfa16d59c23555e1c6542821ea`

Research/manuscript/script authority remains `cmbant/QIprojects`, branch `main`. Lean remains separate in `cmbant/reasoner`, branch `formal-research-lean`.

For every new formal target, fetch live QIprojects main first, read the target-local status/handoff/correction files, and record the exact QIprojects commit used for semantic matching. Do not treat old QIprojects formal branches or `docs/reasoner_head.txt` as current Lean authority.

## Trust/status vocabulary

Use these labels deliberately:

- `PAPER / SOURCE`: current QIprojects statement.
- `EXACT DERIVATION / AUDIT`: restartable exact algebra/certificate outside Lean.
- `FORMALIZED`: Lean source exists.
- `MODULE-GREEN`: the exact module source passed a real `lake build` at the stated head.
- `AGGREGATE-GREEN`: `lake build FormalResearch` passed at the stated exact code head/tree.
- `LEAN-CHECKED`: semantic source match plus the required exact-head Lean gates under the current project discipline.
- `EXTERNAL`: a load-bearing analytic/geometric/special-function input remains outside Lean.

No `sorry` or `admit` is allowed.

Aggregate redness no longer blocks promotion globally: the exact code tree above is aggregate-green. Semantic source matching is still theorem-specific; do not promote a theorem merely because its module builds.

## Source reconciliation against current QIprojects main

The source pin moved repeatedly during this maintenance pass. The final checked pin is `98930c46ed1ddb27237e6e8d37c3b607ad956796`.

### QI-C Endpoint14

Canonical research-side source remains:

`QI-C/code/verify_qubit_ci_tail_proof.py`

The earlier source reconciliation established that this script was byte-identical across the source advance that originally triggered the audit, with Git blob

`e5d44a1733ce0520bebd37a9172348b1476f7463`.

The later QIprojects advance from `eae4a5a48aa2b2e29303d18352a508efe3f35a38` to `98930c46ed1ddb27237e6e8d37c3b607ad956796` did not include QI-C changes. Thus no new source-side Endpoint14 statement superseded the Lean certificate during this pass.

Status at exact Lean head `3e27e2ed...`: the reduced identity and full determinant/Gaussian bridge are source-aligned and aggregate-green. Under the current handoff discipline the Endpoint14 chain may now be called **LEAN-CHECKED** at that exact verified code tree.

Do not revive by default the old dead ends: direct 57 determinant evaluations, large positive tail samples, symbolic 14x14 determinant computation, or repeated Schur-complement determinant evaluation.

### QI-D

Current QIprojects status now records the nested `N_n/n` facet theorem as proved/frozen for all `n >= 5`, with support defect `h_odd-h_even = 2`. Current constructive verification also records the `D4 -> D5` crossing step as giving D5 facet dimension 24.

This is consistent with the finite D5 certificate maintained here: exact support value five after the current normalization, an explicit active set, and 24 independent affine differences. The current QI-D source advance added substantial all-rank and exceptional-type work; it did not invalidate the finite D5 base certificate repaired in Lean.

Do not infer that every newer QI-D theorem is formalized. In particular, the all-rank type-D programme, D4 strict-containment theorem, F4/E6 pointwise-relaxation results, and compact-compression open problems require their own theorem-by-theorem semantic audit.

### Blaschke

The live Blaschke programme still retains the exact Walsh/Wronskian quotient infrastructure. During the later source advance the only Blaschke file visible in the relevant compare was a T09 verification text update, not a replacement of the Walsh/Wronskian theorem package repaired below.

The quartic charge gap remains subject to the source-side reconstruction caveat: Lean downstream algebra is not by itself a proof of any missing analytic/raw-reconstruction layer. Degree-five Hodge stability remains outside the checked Lean claims unless separately audited.

### Gaudin

QIprojects advanced in several Gaudin P1–P4 directions during this pass. Those source changes do not retroactively turn the repaired matrix-tree/Hamming compatibility modules into proofs of new Gaudin results. For any new Gaudin target, audit the current project-local statement before formalizing it.

## Aggregate-maintenance history

The maintenance pass proceeded by exposing concrete compiler failures, repairing only those failures, requiring a narrow green gate, and then rerunning the full aggregate.

### First repaired frontier

The initial exact blockers were:

1. `FormalResearch/Blaschke/WalshBasis.lean`
2. `FormalResearch/Blaschke/WalshWronskianExactRank.lean`
3. `FormalResearch/Gaudin/MatrixTreeHammingTriangle.lean`

Repairs included current mathlib API migration (`Module.Basis`), robust finite-sum rewrites, and removal of stale simplification/identifier paths. The three-module narrow gate passed before promotion.

### Second repaired frontier

The next full aggregate exposed:

1. `FormalResearch/Blaschke/WalshWronskianKernelBasis.lean`
2. `FormalResearch/Gaudin/MatrixWeightedMetricBridge.lean`

The Gaudin bridge was migrated to the current finite-index/basis API. The Walsh kernel proof was repaired using explicit character orthogonality rather than stale definitional simplification. A five-module narrow gate passed at `f012db9b5febbfa77947df115685b64be7e9ef08` before promotion.

### Later Blaschke/bridge frontier

Subsequent aggregate maintenance also reached and repaired the downstream Blaschke/bridge layer that, immediately before QI-D work, was represented in the narrow frontier by:

- `WronskianAtomIndependence`;
- `WronskianAtomNonvanishing`;
- `WronskianAtomUnitDisk`;
- `SingletonWalshSynthesis`;
- `SingletonKernelDirectSum`;
- `B2BlaschkeRangeEquiv`.

These should be treated as repaired/green maintenance dependencies at the current verified tree. Do not reopen them unless a new compiler regression or source mismatch appears.

## QI-D finite-certificate repair

The aggregate then reached the QI-D D5 certificate chain. The repaired source files are:

- `FormalResearch/QID/D5AffineRank.lean`
- `FormalResearch/QID/D5ActiveAffineCertificate.lean`
- `FormalResearch/QID/D5CharacteristicZeroCertificate.lean`
- `FormalResearch/QID/D5AffineIndependence.lean`
- `FormalResearch/QID/D5SupportingFacet.lean`

`D5FacetCertificate` and `D5FullFacetCertificate` are downstream validation targets and required no semantic statement changes.

### `D5AffineRank`

The large 24x24 inverse audit no longer uses the slow monolithic `native_decide` proof. The right inverse is checked entrywise:

`ext i j; fin_cases i <;> fin_cases j <;> decide`

and the left inverse is obtained from the right inverse via `mul_eq_one_comm`.

### `D5ActiveAffineCertificate`

The selected-active theorem now supplies explicit finite decidability for the quantified proposition rather than relying on the rejected/default native path.

The 24x24 active-difference inverse is likewise checked entrywise, and the left inverse is derived from the right inverse.

The explicit certificate data themselves were not regenerated or changed during maintenance.

### `D5CharacteristicZeroCertificate`

The mod-3-to-integer determinant bridge now states the mapped-matrix equality explicitly by extensionality instead of relying on a non-definitional `simpa` coercion normalization.

The rational determinant bridge similarly uses an explicit rewrite through `RingHom.map_det`.

### `D5AffineIndependence`

The affine reindex proof now includes the current `finSuccAboveEquiv_apply` simplification needed by mathlib 4.34-era elaboration. The underlying affine-independence statement is unchanged.

### `D5SupportingFacet`

The parameterized `native_decide` support proof was replaced by reuse of the already-closed exhaustive Boolean audit `supportUpperCheck_passes`, converted with `of_decide_eq_true`.

A file-local

`set_option maxRecDepth 10000`

is required for current Lean elaboration/linting of this finite wrapper. This is a compiler-resource setting, not a mathematical assumption.

## QI-D validation evidence

The final committed-source narrow gate was run `34313034207` on

`34cc436b3f31d29123ff1d7228018ea3807cfd5e`.

It passed placeholder rejection and built together:

- `FormalResearch.QID.D5AffineRank`
- `FormalResearch.QID.D5ActiveAffineCertificate`
- `FormalResearch.QID.D5FacetCertificate`
- `FormalResearch.QID.D5CharacteristicZeroCertificate`
- `FormalResearch.QID.D5AffineIndependence`
- `FormalResearch.QID.D5SupportingFacet`
- `FormalResearch.QID.D5FullFacetCertificate`

Importantly, this run used the committed `.lean` files directly. The temporary CI source-rewrite shim used during diagnosis had already been removed.

PR #7, **Repair QI-D finite facet certificate chain**, merged that exact source tree into `formal-research-lean` at

`8ef0b96dc41127ac2712d686d6728703259fd7c8`.

The merge tree is exactly the same as the green branch tree:

`53ade79e523314cfa16d59c23555e1c6542821ea`.

## Exact final CI evidence

A source-identical marker commit

`3e27e2ed6e0ea0671335c428fe80178b9c1f6639`

with tree

`53ade79e523314cfa16d59c23555e1c6542821ea`

triggered the hard canonical workflows.

### Failure-only frontier

Run `34313366655` completed successfully. It passed, on the exact marker head:

- placeholder rejection;
- QI-C qubit-tail determinant;
- QI-A three-copy binomial bridge;
- QI-B2 Krawtchouk generating check;
- QI-A printed chirality matrix;
- QI-C all-L qubit-tail nonvanishing;
- Gaudin Bernoulli inverse recurrence;
- Gaudin restricted cross-rank;
- Blaschke product Wronskian;
- Racah Weyl-cell telescoping;
- QI-B2 Hamming Boolean-cube phase;
- QI-B1 apolar full-rank;
- QI-C Endpoint14 one-point elimination smoke.

### Endpoint14 + full aggregate

Run `34313366768` completed successfully on the same exact head/tree. It passed:

- placeholder rejection;
- QI-C Endpoint14 reduced polynomial identity;
- QI-C Endpoint14 full determinant bridge;
- `lake build FormalResearch`.

Therefore the exact code tree at `3e27e2ed...` / `53ade79e...` is **AGGREGATE-GREEN**.

This supersedes all earlier handoff statements that the aggregate was red or timed out.

## What is and is not promoted now

### Promoted by this maintenance pass

- the full `FormalResearch` corpus is aggregate-green at the exact verified code tree;
- Endpoint14 is current-source-aligned and passes its exact hard gate plus the aggregate;
- the repaired D5 finite certificate chain is module-green as committed source and survives the full aggregate;
- the previously repaired Blaschke/Gaudin/bridge maintenance frontier survives the failure-only and full aggregate checks.

### Still requires target-specific semantic work

Do not use aggregate-green status to overclaim newer research results. In particular:

- QI-B/B4 flagship complex-orbit fidelity claims remain governed by live source status;
- the full Blaschke analytic/reconstruction chain is not implied by downstream Lean algebra;
- new q-Weyl/Hahn analytic results are not formalized merely because Racah finite-lattice modules build;
- QI-D all-rank, D4 strict-containment, F4, and E6 results need dedicated Lean statements and semantic audits if chosen as targets;
- compact-compression universality claims that are still open in QIprojects remain open.

## Immediate restart priorities

Track A aggregate maintenance is complete at the verified code tree. Do not continue mechanically fixing old blockers unless the aggregate becomes red again.

For the next Lean work item:

1. fetch live `QIprojects/main` and `reasoner:formal-research-lean` and record both SHAs;
2. if QIprojects moved beyond `98930c46ed1ddb27237e6e8d37c3b607ad956796`, inspect the target-local delta before reusing any semantic pin;
3. choose a source-authorized theorem rather than a compiler-maintenance target;
4. record the exact current source statement and any external assumptions;
5. use narrow module gates while developing, then preserve aggregate-green status before global `LEAN-CHECKED` promotion.

The older T20 small-target roadmap remains useful context, but QIprojects has continued to advance. Re-read the live task/status files before choosing among D4 strict containment, other QI-D finite rational certificates, invariant-exponent targets, orbit-ideal support, or Blaschke composition identities.

## Minimal restart checklist

At the start of any new session:

- fetch live `QIprojects/main` and `reasoner:formal-research-lean`;
- treat `98930c46...` and `3e27e2ed...` as historical pins once either branch moves;
- read `QIprojects/AGENTS.md` and the current target-local status/handoff;
- inspect exact-head GitHub Actions steps, not badges alone;
- keep source statement and Lean theorem status separate;
- do not introduce `sorry` or `admit`;
- do not revive the old Endpoint14 brute-force paths unless the current cached certificate route itself breaks;
- preserve the distinction between finite exact certificates and external analytic/geometric inputs.

Current shortest honest summary:

**At QIprojects source pin `98930c46...`, the maintained reasoner code tree `3e27e2ed...` is exact-head `AGGREGATE-GREEN`; Endpoint14 is source-aligned and Lean-checked under the current discipline; the QI-D D5 finite certificate and the prior Blaschke/Gaudin maintenance frontier are green, so the next work should return to live source-driven formalization rather than aggregate repair.**

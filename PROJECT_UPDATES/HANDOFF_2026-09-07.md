# Formal research handoff — 2026-09-07

This is the current restart point for `cmbant/reasoner:formal-research-lean` after reconciling the Lean tree against live `cmbant/QIprojects:main` and re-running the hard Endpoint14 / aggregate CI path.

It supersedes `PROJECT_UPDATES/HANDOFF_2026-09-06.md` for current status. Keep the 2026-09-06 file as historical context and for the detailed Endpoint14 dead-end record.

## Exact repository heads

At handoff preparation:

- research/source authority: `cmbant/QIprojects:main`
  - `f7bb9cabb277515b8597150e06ec0b30fb049861`
  - latest commit message: `T2 complete: bounded-width quantum 3j and one-small quantum 6j are analytic in h²`
- Lean/formalization branch: `cmbant/reasoner:formal-research-lean`
  - `5e38c2bc55e34e36c28ef6a9b07b0e586c2ab655`

The old handoff pinned QIprojects at

`4160942c260c2a2bdcd480c2cfa5ae5eca810721`.

Current `QIprojects/main` is **381 commits ahead** of that source snapshot. Do not reuse the old pin as a claim that the rest of QIprojects is current.

## Source authority and cross-repository discipline

Research/manuscript/script authority is `cmbant/QIprojects`, branch `main`. Lean remains separate in `cmbant/reasoner`, branch `formal-research-lean`.

Do not copy manuscript/source authority back into the Lean repository. For every new formal target, read the current QIprojects project-local handoff/status/correction first and record the exact QIprojects commit used for semantic matching.

The old QIprojects formal handoff branches are not current source authority. In particular, `docs/agent_tasks/T20_lean_small_targets.md` on current main explicitly notes that:

- the 2026-09-06 formal-research handoff notes are not on `main`;
- the old `docs/lean_status.md` exists only on the stale `formal-research-sync` branch;
- `docs/reasoner_head.txt` on QIprojects main records only the last audit head, not the current reasoner head.

As of this handoff, `QIprojects/docs/reasoner_head.txt` still contains

`938a74aa5a4529acfaf64d6ffbfb9f493a8f6ed1`,

so do not use it as the current Lean head. The authoritative current Lean status is this handoff plus exact GitHub Actions evidence in `cmbant/reasoner`.

## Trust/status vocabulary

Use these labels deliberately:

- `PAPER / SOURCE`: current QIprojects statement.
- `EXACT DERIVATION / AUDIT`: restartable exact algebra/certificate outside Lean.
- `FORMALIZED`: Lean source exists.
- `MODULE-GREEN`: the exact module source passed a real `lake build` at the stated head.
- `AGGREGATE-GREEN`: `lake build FormalResearch` passed at the stated head.
- `LEAN-CHECKED`: semantic source match plus the required exact-head Lean gates under the current project discipline.
- `EXTERNAL`: a load-bearing analytic/geometric/special-function input remains outside Lean.

No `sorry` or `admit` is allowed.

Current QIprojects task guidance is stricter than some older notes: do **not** promote claims to `LEAN-CHECKED` while the aggregate remains red. Module-level progress should still be recorded honestly as `MODULE-GREEN`.

## QI-C Endpoint14: current semantic match

Canonical research-side source remains:

`QI-C/code/verify_qubit_ci_tail_proof.py`

This is the most important result of the QIprojects reconciliation: the file is **byte-identical** between the old source pin and current `QIprojects/main`.

Its Git blob SHA is unchanged:

`e5d44a1733ce0520bebd37a9172348b1476f7463`.

Therefore the 381-commit advance of QIprojects does **not** supersede the Endpoint14 matrix/factorization source used by the current Lean proof.

The current script still supplies the same all-`n` qubit `c=i` tail certificate, the fixed endpoint block, the 14x14 minor, and the exact cubic/quartic determinant-factor argument.

## Endpoint14 formal status

At current reasoner head

`5e38c2bc55e34e36c28ef6a9b07b0e586c2ab655`,

GitHub Actions run `34135757656` passed, on that exact head:

- placeholder rejection;
- `FormalResearch.QIC.Endpoint14ReducedPolynomialIdentity`;
- `FormalResearch.QIC.Endpoint14PolynomialIdentity`;
- `FormalResearch.QIC.GaussianEndpointNonsingular`;
- `FormalResearch.QIC.GaussianEndpointFullRank`;
- `FormalResearch.QIC.GaussianEndpointInjective`;
- `FormalResearch.QIC.GaussianEndpointAdjugateReconstruction`.

The reduced proof still uses the 29 exact elimination certificates and degree-28 interpolation. The full identity is restored by the universal entrywise `X^2` factor and `Matrix.det_smul`, giving the `X^28` determinant factor. The obsolete direct 57-evaluation route is not in the aggregate dependency chain.

Status: **source-aligned and MODULE-GREEN through the full Gaussian endpoint bridge**. Under current QIprojects T20 discipline, do not call it globally `LEAN-CHECKED` until the aggregate is green.

Do not revive by default the known dead ends from the 2026-09-06 handoff: direct 57 determinant evaluations, large positive tail samples, symbolic 14x14 determinant computation, or repeated Schur-complement determinant evaluation.

## Aggregate CI: latest exact state

The source tree at commit `bb4e0fe1a58dc354a1ac970dbd1186745dfbe34e` passed both:

- targeted aggregate-frontier run `34135297117`;
- ordinary failure-only frontier run `34135297003`.

Commit `5e38c2bc55e34e36c28ef6a9b07b0e586c2ab655` is an empty marker commit on the same source tree, used to trigger the full Endpoint14 + `FormalResearch` build.

Full run `34135757656` again passed every Endpoint14-specific stage, then entered

`timeout 1200 lake build FormalResearch`.

The aggregate still failed and ultimately timed out with exit code 124. The current concrete blockers are now much narrower than in the previous handoff:

1. `FormalResearch/Blaschke/WalshBasis.lean`
   - parse/syntax failure around line 38;
   - a later sum-distribution rewrite no longer matches;
   - old unqualified `Basis` API still appears and must use current `Module.Basis`/current construction API.
2. `FormalResearch/Blaschke/WalshWronskianExactRank.lean`
   - unknown identifier `hjk`;
   - removed/renamed `smul_sum` simplification path.
3. `FormalResearch/Gaudin/MatrixTreeHammingTriangle.lean`
   - unqualified `Basis` at both theorem layers; same current-mathlib migration already applied successfully in the preceding matrix-tree module.

Because Lake continued compiling independent targets after these errors, the 1200-second timeout does not prove there are no deeper blockers. Fix these three first, add them to a narrow targeted gate, then re-run the full aggregate.

### Aggregate-maintenance modules already repaired and observed green

The following previously surfaced compatibility blockers now build successfully in the current aggregate path or targeted gates:

- QI-A: `PairingToPrintedBasis`, `PrintedChiralitySpectrum`, `ThreeCopyAlgebraBinomialBridge`, `ThreeCopyChiralityPersistence`;
- Blaschke: `ZeroFlipWronskian`, `WalshWronskianLinearMap`;
- Gaudin: `CrossPoissonMatrixRank`, `BernoulliInverseUniqueness`, `MatrixTreeRankTriangle`;
- QI-B2 / bridges: `HammingRadialGate`, `HammingCanonicalGateAction`, `B2BlaschkeWalshGauge`;
- QI-B1: `ApolarKernelDimension`, `ApolarCoherentKernel`, `ApolarRealSpan`.

Do not reopen these unless a new compiler or semantic-source mismatch appears.

## What changed recently in QIprojects and what it means for Lean

The 381-commit source advance is not one coherent theorem update. The main formalization-relevant changes are grouped below.

### 1. QI-A extension roadmap was substantially expanded

Current source:

`QI-A/notes/EXTENSION_PROJECTS.md`

Important current priorities include:

- audit/finish immediate-classicalization and information-hierarchy results before QI-A submission;
- flagship invariant Chernoff exponent, with explicit warning that the naive compact-orbit exponent statement is false in general;
- four-qubit generic three-copy reconstruction as the highest QI-C structural target;
- all-length qutrit `d_loc=3` beyond the current finite-length frontier;
- approximate deferred-query memory and chiral tangent Fisher-information follow-ons.

These are source/research priorities, not automatically current Lean theorems.

### 2. QI-B/B4 orbit-Bergman advanced dramatically, but the flagship inequality is still open

Current handoffs:

- `QI-B/HANDOFF_2026-09-06.md`
- `QI-B/B4-orbit-bergman/README_HANDOFF.md`

The active B4 route now has many exact reductions: common-Schur theorems, spin-channel decomposition, one-unitary wall, two-boost endpoint reduction, aligned Schur reductions, and numerous exact SymPy certificates.

However the flagship source explicitly still says **do not claim the theorem yet**. The key complex-orbit fidelity target

`F_C(psi_+ | X_{psi_-}) = 1/2`

remains open in the full problem. Do not turn the numerical/noncompact evidence into a Lean theorem statement.

The reasoner B2/Blaschke bridge modules are not a formalization of this full B4 complex-orbit theorem.

### 3. QIprojects now gives an explicit five-target Lean roadmap (T20)

Current source:

`docs/agent_tasks/T20_lean_small_targets.md`

After the QI-C endpoint certificate is green, the requested small targets are:

1. finite-group invariant exponent theorem (Gram/Gershgorin plus closest-pair converse);
2. qubit `U(1)` counterexample with `-log F_K = 2 xi_inv` for the complementary Bernoulli pair;
3. orbit-ideal support lemma `W_{y,d} = I_d(X_y)^perp`;
4. `DS(W(D_4))` strictly contains `conv W(D_4)` using the exact rational witness and LP dual;
5. Blaschke composition superadditivity constants plus the B3 polygon-facet generating-function identity.

Current reasoner `FormalResearch/Bridges` contains the B2/Blaschke bridge family, not dedicated modules for T20 targets 1--4. Current `FormalResearch/QID` is D5-focused; there is no dedicated D4 strict-containment module yet.

Target 5 may overlap ingredients in existing Blaschke composition modules, but it has not been semantically audited against the exact T20 statement and should not be counted complete without that audit.

### 4. QI-D has a new exact-rational D4 strengthening certificate

Current source:

`QI-D/notes/operator_kostant/DS_STRICTLY_CONTAINS_CONV_D4.md`

Status there is **proved (exact rational certificate with exact LP dual)**. It gives a finite 2304-inequality characterization, an explicit rational witness `A`, an exceptional facet functional `F`, and exact optimum `7/6` over `DS(W(D_4))`.

This is an especially suitable Lean target because the load-bearing part is finite rational linear algebra. It is not yet represented by the current D5-only QID module set.

### 5. Blaschke v13 is now through Turn 14

Current source:

`Blaschke/HANDOFF.md`

The live direction now includes target-free Wronski normalization, Van Vleck reconstruction, and the third-transvectant identity organizing the polynomial-plane data. The active reconstruction pipeline is described schematically as

`C -> R_hat -> S_hat,L_hat -> E`.

Important claim discipline is unchanged: the quartic charge gap remains **PROOF DRAFT** at the research-source level because the raw upstream reconstruction is not yet a clean restartable canonical certificate. The Lean `QuarticChargeGapAlgebra` module formalizes only the downstream polynomial/sign/unsquaring layer and must not be advertised as a proof of the whole analytic/raw-reconstruction chain.

The degree-five Hodge-stability claim remains open/numerical; do not promote it.

### 6. Weyl target T2 is now complete on the source side

Current `QIprojects/main` head itself is the T2 completion commit. New standalone notes under `ResearchNotes/Weyl/` prove source-side that:

- the one-small quantum `6j` bounded-width kernel is analytic in `h^2`;
- the two-large quantum `3j` bounded-width kernel, after the exact Weyl row gauge, is analytic in `h^2`;
- exact reflection/gauge structure and hyperbolic limiting kernels are recorded with verification scripts/logs.

Relevant paths:

- `ResearchNotes/Weyl/q6j_bounded_width_even/`
- `ResearchNotes/Weyl/q3j_bounded_width_even/`

The existing reasoner `FormalResearch/Racah` layer covers finite-lattice/Weyl-cell algebra and bridges; it is **not** a formalization of these new analytic q-Weyl T2 theorems. Treat the analytic/continuation/special-function content as source-only / `EXTERNAL` until dedicated Lean work exists.

### 7. New standalone antiunitary-odd invariant note

`ResearchNotes/QI/antiunitary_odd_invariants/` now gives a general Frobenius-Schur-type count of antiunitary-odd invariant observables and recovers scalar spin chirality as a special case. This is new source material and is not currently represented by a dedicated Lean module.

### 8. QI-B/B2 Hamming source was cleaned/reindexed

`QI-B/B2-hamming/HANDOFF_MANIFEST.md` now records canonicalized v25/v26 duplicates and the current heat-kernel source/layout. The current Lean Hamming modules should continue to be matched theorem-by-theorem; do not infer a semantic change merely from file renames/duplicate cleanup.

## Immediate restart priorities

There are now two legitimate tracks; do not conflate them.

### Track A — finish aggregate maintenance

If the goal is `AGGREGATE-GREEN` / eventual global `LEAN-CHECKED` promotion:

1. fix `Blaschke/WalshBasis.lean`;
2. fix `Blaschke/WalshWronskianExactRank.lean`;
3. fix `Gaudin/MatrixTreeHammingTriangle.lean`;
4. build those three together in a narrow hard gate;
5. re-run the existing Endpoint14 + `lake build FormalResearch` workflow;
6. continue only from exact compiler failures revealed by that run.

### Track B — follow the new QIprojects T20 source roadmap

The endpoint prerequisite is satisfied at module level, so the five T20 targets can be developed independently and kept `MODULE-GREEN` without waiting for the whole corpus aggregate to be repaired.

Best low-risk first candidate is T20 target 4, the exact-rational D4 strict-containment certificate. T20 targets 1--3 are conceptually clean but introduce more orbit/invariant geometry; target 5 should first audit what existing Blaschke composition modules already prove.

## Minimal restart checklist

At the start of any new session:

- fetch live `QIprojects/main` and `reasoner:formal-research-lean` and record both SHAs;
- if QIprojects moved, compare from `f7bb9cabb277515b8597150e06ec0b30fb049861` and inspect only changed project-local status/source files relevant to the target;
- read `QIprojects/AGENTS.md` and the current project-local handoff;
- do not rely on stale QIprojects `docs/reasoner_head.txt` or branch-only `docs/lean_status.md`;
- inspect exact-head GitHub Actions steps, not workflow badges alone;
- classify source statement and Lean theorem separately;
- preserve the green Endpoint14 bridge and its cached certificate path;
- do not claim B4 `F_C=1/2`, the full Blaschke quartic chain, degree-five Hodge stability, or new q-Weyl T2 analytic theorems as Lean-checked without dedicated semantic/formal evidence.

Current shortest honest summary:

**QI-C Endpoint14 is current-source-aligned and module-green through the full Gaussian bridge; QIprojects has advanced substantially in B4, Blaschke, QI-D and Weyl; the full Lean aggregate is still red on three known downstream modules; the new source-side Lean roadmap is T20's five small targets.**

# Formal research handoff — 2026-09-09

This supersedes the restart/status portion of `PROJECT_UPDATES/HANDOFF_2026-09-07.md` after reconciling the maintained Lean tree against live `cmbant/QIprojects:main` and formalizing the newly closed quartic charge-gap reconstruction bridge.

## Exact source and Lean verification pins

Current research/source authority checked in this pass:

- `cmbant/QIprojects:main`
- `a32a35809edd12b7d7cb7c23b332edad36efa5bc`

The quartic Lean source was hard-verified at:

- `cmbant/reasoner:agents/blaschke-quartic-raw-bridge`
- exact code head `1dc24e93c1483782dd80b162c9b48cd26707fc41`
- GitHub Actions run `34336956345`

That run passed:

1. placeholder rejection;
2. `lake build FormalResearch.Blaschke.QuarticChargeGapReconstructionBridge`;
3. `lake build FormalResearch`.

Thus the exact Lean code at `1dc24e93...` is **MODULE-GREEN** for the new bridge and **AGGREGATE-GREEN** for the full corpus. Documentation/CI-only cleanup commits may follow that verified code head; do not confuse them with a new unverified Lean source tree.

## Why the previous plan changed

The September 7 handoff correctly warned that the quartic charge gap could not be promoted from the existing downstream algebra alone because the raw charge-matrix-to-factorized-defect reconstruction was not restartably closed.

That warning is now stale on live QIprojects. The source theorem is promoted in:

- `Blaschke/CLAIM_STATUS.md`;
- `Blaschke/notes/quartic_charge_gap_closure_2026-09-09.md`;
- `Blaschke/code/cycle4/v13_quartic_raw_reconstruction.py`;
- `Blaschke/verification/v13_quartic_raw_reconstruction.txt`.

The source result is: for every degree-4 finite Blaschke product, if `mu1 >= mu2 >= 0` are the two positive charge singular values, then `mu1-mu2 >= 1/2`.

The newly added Lean module

`FormalResearch/Blaschke/QuarticChargeGapReconstructionBridge.lean`

matches the scalar reconstruction formula from current QIprojects and formalizes:

- the squared factor `G`;
- the denominator factor `Delta`;
- the exact scalar raw prefactor;
- nonpositivity of that prefactor on the physical scalar domain;
- combination with the already-formalized `F1/F2/F3` sign chain;
- nonnegativity of the squared-gap defect;
- the final unsquaring to `1/2 <= mu1-mu2`.

The exact CAS reconstruction equality remains an explicit hypothesis in Lean. The Bessel/Gram input, Schur-domain input, Möbius normalization, raw residue/Hilbert-metric construction, and confluent-continuity step also remain external. Therefore this bridge is Lean-checked conditional scalar algebra matching the now-proved source theorem; it is not a claim that the entire analytic/CAS proof has been reimplemented in Lean.

## QI-A reconciliation

QI-A changed materially after the previous source pin. It is now the sole live submission manuscript for the merged QI-A/QI-C copy-complexity and quantum-memory programme. Current authority is:

- `QI-A/paper/finite_copy_quantum_memory.tex`;
- `QI-A/notes/handoff_readme.md`;
- `QI-A/notes/T01_REPORT.md`;
- `QI-A/VERIFICATION_SUMMARY.md`.

The authoritative integration run is `34321974914`; the validated manuscript commit recorded by QIprojects is `988eaba1bd4593ab7ff16cdf84cee2616e8e54d5`.

New or strengthened exact source statements that are not all represented by the current Lean tree include:

- exact reversible coherent width with free classical labels, `w_min = max_alpha dim M_alpha`;
- the heterogeneous fixed-copy product-character formula and party-type rate;
- exact `d=3` local-Schur overhead: ratio `6` for genuinely mixed qubit/high-dimensional architectures, with exact homogeneous endpoint corrections;
- corrected measured-Chernoff values `Q_meas,cond=7/11`, `Q_meas,full=21/22`, `xi_meas=log(22/21)`;
- the six-level information hierarchy;
- SWAP-cost-four minimality and the exact `17/32` local-access benchmark.

The current reasoner QI-A modules still provide substantial compatible infrastructure, especially the `S3` character formulas and all-party three-copy memory identities. They should not be described as formalizing every newly consolidated QI-A theorem.

The cleanest next QI-A Lean target is the exact `d=3` local-Schur overhead certificate from `QI-A/scripts/verify_local_schur_d3_overhead.py`: it is finite character algebra and should reuse the existing `FormalResearch.QIA.ThreeCopyCharacterClosedForms` infrastructure.

## QI-C reconciliation

QI-C's role changed, but the canonical Endpoint14/all-`m` tail source did not.

The live file

`QI-C/code/verify_qubit_ci_tail_proof.py`

still has Git blob

`e5d44a1733ce0520bebd37a9172348b1476f7463`,

identical to the previous handoff source. Therefore the existing Endpoint14 and Gaussian bridge semantic match is not invalidated by the QI-C restructuring.

QI-C is now explicitly a research/provenance/certificate package. Its duplicate submission manuscript was removed, and `run_all_checks.py` now runs all eight live certificate scripts and records logs under `QI-C/verification/`. These are source-tree and reproducibility improvements, not changes to the Endpoint14 polynomial certificate.

Do not infer that every QI-C certificate is Lean-formalized merely because the QI-C aggregate runner is green. In particular, the many-qubit local-identifiability theorem and SWAP-cost certificate need theorem-specific Lean audits if selected as targets.

## Current next-work order

1. Merge the quartic reconstruction bridge after review, preserving the exact verified Lean source tree evidence above.
2. Formalize the QI-A `d=3` local-Schur overhead character identity as a narrow module target, then preserve aggregate green.
3. Re-audit the strengthened QI-A reversible coherent-width theorem separately before attempting it; its Schmidt-number/recovery lower bound is not just finite arithmetic.
4. Keep QI-C Endpoint14 closed unless its canonical certificate script changes or Lean regresses.
5. Continue to choose future targets from live QIprojects status rather than the old September 6 T20 ordering.

## Claim discipline

- No `sorry` or `admit`.
- Keep PAPER/SOURCE, exact external certificate, FORMALIZED, MODULE-GREEN, AGGREGATE-GREEN, and LEAN-CHECKED distinct.
- A source theorem becoming proved does not automatically make its full analytic proof Lean-checked.
- QI-C manuscript retirement changes ownership/citation, not the meaning of the unchanged Endpoint14 certificate.
- Degree-five Blaschke Hodge stability, primitive all-degree Gate A, Gate B, and the other explicitly open source claims remain open.

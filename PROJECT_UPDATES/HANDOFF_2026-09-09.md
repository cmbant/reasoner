# Formal research handoff — 2026-09-09

This supersedes the restart/status portions of the September 7 handoffs and earlier versions of this file. The Lean programme moved substantially during September 9: live QIprojects source changed, several old T20 targets were completed, QI-D gained all-rank and exceptional-group certificates, and the maintained reasoner branch now contains a materially larger aggregate-green QI-A/QI-B/QI-D/Blaschke frontier.

## Current authoritative heads

Research/manuscript/certificate authority:

- repository: `cmbant/QIprojects:main`
- current live head: `acfb127df5cc5998fb91cd00ea6e5311d1462e39`

Lean authority:

- repository: `cmbant/reasoner:formal-research-lean`
- current maintained head after reasoner PR #16: `28d2a10596b1336e434316d7f24b25e652efa617`

Re-audit both heads before starting new work.

## Exact maintained Lean verification frontier

The newest promoted feature is the QI-D exceptional `F4` finite facet certificate, reasoner PR #16.

Exact Lean-content head before validation-workflow-only commits:

`16ce743be5cd276410c905f33aadc2699d6a12e3`

GitHub Actions run:

`34399984280`

That run passed:

1. rejection of `sorry` / `admit`;
2. `lake build FormalResearch.QID.F4ExceptionalFacetCertificate`;
3. `lake build FormalResearch.QID.F4ExceptionalFacetAffineWitness`;
4. full `lake build FormalResearch`.

PR #16 then merged the same Lean bytes, after temporary-workflow cleanup and status-note addition, into `formal-research-lean` at `28d2a105...`.

Earlier merged features retain their own exact verification evidence:

- PR #8, Blaschke quartic reconstruction bridge: head `1dc24e93c1483782dd80b162c9b48cd26707fc41`, run `34336956345`;
- PR #9, QI-A `d=3` local-Schur overhead: head `2e15ff34467c5de1a2432c6d6bd7c5c7ddad19ed`, run `34339736666`;
- PR #10, QI-A information-hierarchy coefficients: head `3591192788ae917bb43c1f8e7102e95270258efb`, run `34347204321`;
- PR #11, QI-B symmetric `U(1)` exponent-gap certificate: head `297231d4be4780b1d091092a7d841bbbe62edb85`, run `34348733331`;
- PR #12, QI-D D4 finite witness/dual: head `8a95d5ba7fb0b200dfbfb2efb4c97e3710ead8be`, run `34374566836`;
- PR #13, QI-D nested all-rank lower witness: head `a8d883584eb235fa2f1c4af0c01ec309cedfc0ac`, run `34378799353`;
- PR #14, QI-D nested all-rank upper decomposition/support implication: head `df4466a9e5b773db105bb1fcbd14945129d2a33b`, run `34390385283`;
- PR #16, QI-D exceptional F4 finite facet certificate: head `16ce743be5cd276410c905f33aadc2699d6a12e3`, run `34399984280`.

All promoted runs above include a full aggregate build at the cited exact Lean-content head.

## Paper-integration checkpoint rule

If source-proved / Lean-checked work is materially stronger than the current manuscript TeX, do **not** silently edit or merge the paper. Create a QIprojects review branch and open a **draft/unmerged PR** containing:

1. the exact source theorem/certificate and source pin;
2. the exact reasoner verification head/run/merge PR;
3. the Lean trust boundary;
4. proposed manuscript wording / integration location;
5. a validation checklist.

Two such QI-D checkpoints are currently open and intentionally unmerged:

- QIprojects PR #82: all-rank nested DS equality `n+2/3` for every `n>=4`, where the current TeX still states equality only for `n=4,5,6` and a lower bound in higher rank;
- QIprojects PR #84: reproducibility text for the displayed exceptional F4 facet certificate (rank 3, support 1, 36 active Weyl matrices, affine rank 15, folded coordinates), without changing the open compact-F4 theorem status.

Do not merge either review PR automatically.

## Blaschke status

The September 7 quartic warning is obsolete on live QIprojects: the degree-four charge gap is source-proved with restartable exact CAS reconstruction. Reasoner PR #8 added `FormalResearch/Blaschke/QuarticChargeGapReconstructionBridge.lean` and the maintained aggregate contains both the older quartic algebra and the reconstruction/sign/unsquaring bridge.

Lean proves the conditional scalar chain ending in `1/2 <= mu1-mu2` from the exact reconstruction equality and analytic scalar hypotheses. The raw CAS derivation, Bessel/Gram input, Schur-domain theorem, Möbius normalization, residue/Hilbert-metric construction, and confluent-continuity step remain external. Do not call the entire analytic proof internally Lean-derived.

Primitive all-degree Gate A, Gate B, degree-five Blaschke Hodge stability and other source-open claims remain open unless live QIprojects says otherwise.

## QI-A reconciliation

QI-A is the live submission-facing copy-complexity / quantum-memory manuscript.

Important source correction: the hoped-for universal unrestricted-physical block-curve converse is false. Current source separates four memory architectures: strong reconstructive/full-block, deferred-query full-block, unrestricted physical with free classical side information, and sector-label-only physical memory.

Merged Lean coverage during this cycle includes:

- exact three-copy `d=3` local-Schur character identities (`6K=L`, with exact endpoint corrections), PR #9;
- exact scalar information-hierarchy/Chernoff coefficient ordering, PR #10.

Both are already represented in current QI-A TeX; no extra paper-review PR is required for those results.

These modules do not formalize the whole operational QI-A manuscript. T07 finite coding/guessing statements are legitimate future targets only if their message/query/guessing hypotheses are represented faithfully. The reversible coherent-width theorem still requires substantial recovery/entanglement/Schmidt-number infrastructure.

## QI-C reconciliation

QI-C remains a research/provenance/certificate package. Its canonical all-`m` qubit-tail source `QI-C/code/verify_qubit_ci_tail_proof.py` remains unchanged from the earlier audit, with Git blob `e5d44a1733ce0520bebd37a9172348b1476f7463` at the last exact check.

Therefore Endpoint14 maintenance remains closed. The current Lean chain formalizes the determinant/nonvanishing certificate layer; do not identify that automatically with the full geometric `d_loc=3` local-identifiability theorem.

## QI-D: D4 certificate and all-rank nested theorem

### D4 exact finite certificate — merged PR #12

Lean checks the exact finite source data:

- `|W(D4)| = 192`;
- coweight orbit sizes `(8,24,8,8)`;
- exact rational witness/facet pairing `7/6`;
- Weyl support at most `1` with attainment;
- exactly `2304` finite coweight constraints and zero violations;
- the exact printed 14-term rational dual with objective `7/6`.

The finite-coweight characterization of semantic `DS(W(D4))` membership remains a named external bridge.

### Nested all-rank lower witness — merged PR #13

For rank `n=k+4`, Lean defines the actual block normal and nested witness `A_n=I_k direct_sum A_*` and proves

`<N_n,A_n> = k + 14/3 = n + 2/3`.

The generalized-Birkhoff theorem supplying `A_n in DS(W(D_n))` remains external.

### Nested all-rank upper structure — merged PR #14

Lean proves the exact source decomposition

`6N_n = 2R_n + 2C_n + sum_{a=1}^4 s_a t_a^T`,

expands `R_n` into the actual `n+1` basis-row/sign atoms and `C_n` into the five source sign/basis-column atoms, proves the four opposite-parity sign atoms and exact count/budget arithmetic, and derives

`<N_n,T> <= n+2/3`

for an arbitrary test matrix `T` from the individual sharp atom-support inequalities.

The remaining upper semantic input is the arbitrary-real type-D rearrangement / determinant-constrained Procrustes theorem that supplies those individual atom bounds. It is explicit in Lean rather than silently assumed.

Thus PRs #12–#14 formalize the finite D4 certificate plus the exact lower and upper all-rank matrix algebra, but the complete semantic theorem still depends on two external type-D bridges:

1. generalized-Birkhoff `DS(W(D_n))` membership/nesting;
2. the sharp real type-D rank-one support/Procrustes theorem.

Do not label the whole all-rank `DS(W(D_n))` extremal theorem internally Lean-proved until both are closed.

## QI-D T33 exceptional F4 certificate — merged PR #16

Canonical source:

- `QI-D/code/certify_F4_exceptional_facet.py`;
- `QI-D/verification/certify_F4_exceptional_facet.txt`;
- `QI-D/notes/T33_REPORT.md`.

Lean modules:

- `FormalResearch/QID/F4ExceptionalFacetCertificate.lean`;
- `FormalResearch/QID/F4ExceptionalFacetAffineWitness.lean`.

Lean checks:

- the two displayed rational F4 generators reach all `1152` exact Weyl matrices through a three-coset parameterization over the 384 signed-permutation subgroup;
- all 1152 parameterized matrices are distinct;
- transition formulas agree exactly with right multiplication by the source generators;
- rank-one support `1`, active count `288`;
- exceptional rank-three support `1`, active count `36`;
- exact rank-three minor/determinant data and folded E6/F4 coordinate/Gram identities;
- 16 explicit support-active generator words whose 15 projected differences have a checked explicit inverse, yielding a trivial coefficient kernel / affine-rank-15 witness.

External/open inputs remain explicit: the literature theorem that there are exactly two F4 facet orbits, the ambient structural interpretation beyond the concrete certificate, and compact-F4 simultaneous Weyl convexity.

Do **not** infer a compact-F4 theorem from PR #16.

## QI-D T33 E6/F4 A2 envelope-to-quartic source candidate

Canonical source:

`QI-D/code/certify_E6_A2_phase_envelope.py`.

This is now the cleanest remaining source-authorized Lean candidate. It proves/checks exact structural algebra:

- the integer phase map `[[2,-1],[-1,2]]` has determinant `3`, hence gives the relevant degree-three torus cover;
- the third harmonic is exactly `uv`;
- one phase is eliminated by `max_v H = c0 + 2 (Re(a u) + |b+c u|)`;
- with `d=(target-c0)/2`, the bound reduces to `d>=|a|` and `q(u)=(d-Re(a u))^2-|b+c u|^2 >= 0` on the unit circle;
- tangent-half-angle substitution gives a real polynomial of degree at most four, with exact rational coefficient formula;
- the source uses square-free factorization + Sturm root counts as an exact rational nonnegativity engine, plus the `t=infinity` check;
- exact positive and negative regressions are included.

Critical scope boundary: this **does not** prove that all genuine compact-F4 endpoint coefficient tuples satisfy the quartic positivity condition, and therefore does not prove compact-E6 simultaneous Weyl convexity. T48 remains the research-side endpoint-locus problem.

A Lean implementation should target the exact algebraic reduction/certificate engine only, not claim the open endpoint-locus theorem.

## QI-B/B4 late source audit

T34 proves an exact Fierz/Pluecker reformulation and an exact spectral obstruction to a proposed stronger route, but **does not** prove the flagship B4 bound/equality. The stronger spectral proposal is explicitly refuted by an exact witness; the flagship remains open.

Therefore T34 is useful source structure, not a source-authorized completion target. Do not launch a Lean branch claiming the B4 flagship has been proved.

## Current next-work order

Re-audit live heads before every item. At this handoff the preferred order is:

1. **QI-D T33 E6/F4 A2 envelope-to-quartic reduction.** Formalize the exact algebraic reduction/certificate engine only. Keep uniform compact-endpoint positivity explicitly open.
2. **QI-A T07 exact finite coding/guessing framework**, only if the operational hypotheses can be represented faithfully.
3. **General type-D support/Procrustes bridge**, if the necessary real-matrix/singular-value infrastructure is tractable enough to close the remaining all-rank upper semantic input.
4. Revisit QI-B/B4 only from a new source-proved theorem/certificate; T34 itself stops at an obstruction/reformulation.

T48 remains research-side and is not a Lean target until it produces a source-proved endpoint-locus theorem/certificate.

## Restart rules

1. Read this handoff, then current QIprojects `docs/lean_status.md` and `docs/agent_tasks/T20_REPORT.md`.
2. Recheck both repository heads before coding.
3. Read the theorem-local source note/script/log for the proposed target.
4. Start a fresh `agents/<task>-<short-name>` branch from current `formal-research-lean`.
5. Reject `sorry`/`admit`, hard-build the smallest changed module first, then import it into `FormalResearch.lean` and hard-build the full aggregate before promotion.
6. Record exact source pin, exact reasoner verification head, Actions run and external assumptions in the PR/update docs.
7. Merge only the exact tested code tree (or a source-identical cleanup tree whose Lean bytes are unchanged).
8. If Lean/source work materially exceeds current TeX, open a separate **draft/unmerged QIprojects review PR** before any manuscript integration.

## Claim discipline

- Keep SOURCE/PAPER, exact external certificate, FORMALIZED, MODULE-GREEN, AGGREGATE-GREEN, MERGED and full semantic theorem status distinct.
- Aggregate green proves compilation of the stated Lean theorem, not every analytic/operational/geometric premise surrounding it.
- Preserve source corrections and counterexamples; never restore a refuted claim to simplify formalization.
- Do not infer semantic `DS` membership from a finite certificate or exact pairing without the relevant bridge theorem.
- Do not turn an open research task into a Lean target merely because it is strategically important.
- Do not turn a finite F4 Weyl-polytope certificate or the E6/F4 quartic reduction into a compact exceptional-group theorem.

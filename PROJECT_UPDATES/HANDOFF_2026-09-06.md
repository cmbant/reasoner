# Formal research handoff — 2026-09-06

This is the current restart point for `cmbant/reasoner:formal-research-lean` after the archive-to-GitHub migration and the Lean maintenance / QI-C endpoint work.

## Source authority

Research/manuscript/script authority is now **`cmbant/QIprojects`**, default branch `main`.

At handoff preparation, live `QIprojects/main` was:

`4160942c260c2a2bdcd480c2cfa5ae5eca810721`

Do not restart by scanning old Library ZIP version numbers. Use QIprojects paths and provenance files first; archived ZIP/file IDs are historical ingest provenance only.

Lean remains intentionally separate in this repository on branch `formal-research-lean`. Do not merge either repository's default branch without explicit instruction.

## Lean environment and trust rule

The project pins Lean in `lean-toolchain` and pins mathlib in `lakefile.toml`. CI history before the hard-gate repair contained workflow-level green runs with Lean failures masked by `continue-on-error`; do not restore that interpretation.

Use these statuses:

- `FORMALIZED`: source exists.
- `MODULE-GREEN`: the exact module source passed a real `lake build` on the stated head.
- `AGGREGATE-GREEN`: `lake build FormalResearch` passed on the stated head.
- `LEAN-CHECKED`: the exact current statement/source matches the intended research claim and the relevant real build gate passed.
- `EXTERNAL`: a required analytic/operator/special-function input is intentionally outside the present Lean proof.

No `sorry`/`admit` is allowed.

## Current exact Endpoint14 evidence

At source head

`e73babebe4b2a97b79a0168e7052c0fd027d3472`

GitHub Actions run `34058351961` successfully completed all Endpoint14-specific hard gates before the aggregate step:

- placeholder rejection;
- `FormalResearch.QIC.Endpoint14ReducedPolynomialIdentity`;
- `FormalResearch.QIC.Endpoint14PolynomialIdentity`;
- `FormalResearch.QIC.GaussianEndpointNonsingular`;
- `FormalResearch.QIC.GaussianEndpointFullRank`;
- `FormalResearch.QIC.GaussianEndpointInjective`;
- `FormalResearch.QIC.GaussianEndpointAdjugateReconstruction`.

The reduced identity reused the exact cached build of the 29 elimination certificates. The downstream determinant bridge completed successfully on the same exact head.

Therefore the Endpoint14 route is now **MODULE-GREEN through the full Gaussian downstream bridge**. This supersedes the older resource-obstruction description below. Do not describe Endpoint14 itself as the current blocker.

## QI-C Endpoint14: exact state

Canonical research-side semantic source:

`QIprojects/QI-C/code/verify_qubit_ci_tail_proof.py`

It specifies the fixed 14x14 minor and exact determinant factorization. Lean data were migrated to the exact same coefficient table.

### What is complete

1. `Endpoint14Data.lean` contains the fixed integer matrix data.
2. `Endpoint14ReducedData.lean` proves the migrated entries have zero degree-0 and degree-1 coefficients, so entrywise `A(t) = t^2 A_red(t)`.
3. Hence the full determinant has the universal factor `t^28`, and the reduced determinant has degree at most 28.
4. The interpolation sample set was reorganized into 29 exact rational elimination certificates with small tail values so every sample certificate can compile within practical CI limits.
5. `Endpoint14EliminationCertificate` is available through compiled split certificate dependencies.
6. `Endpoint14ReducedPolynomialIdentity.lean` proves the reduced degree-28 determinant identity by interpolation from those 29 samples.
7. `Endpoint14PolynomialIdentity.lean` restores the full determinant using the `t^28` scalar factor and the matrix determinant scaling bridge.
8. The physical Gaussian endpoint modules now prove nonsingularity, full rank, injectivity, and adjugate-based scaled reconstruction.
9. CI run `34058351961` passed the full Endpoint14-specific bridge on exact head `e73babeb...`.

### Endpoint14 status

The Endpoint14 proof chain is no longer blocked by the old 29-sample serialization problem. Treat the exact modules above as real module-green evidence. Full-corpus `AGGREGATE-GREEN` status is still pending for unrelated reasons described below.

### Dead ends already tested — do not repeat by default

- 57 direct 14x14 `native_decide` determinant evaluations: factorial-scale / infeasible.
- repeated 7x7 Schur-complement evaluation: still too expensive in aggregate.
- symbolic `eval_det` / Bird 14x14 polynomial determinant: hosted runner killed during computation.
- generic executable Bird determinant on even one reduced numeric 14x14 sample: exceeded the practical CI budget.
- large positive interpolation tail samples near 26--30: exact rational arithmetic became prohibitively expensive; small negative tail samples were used instead.

## Aggregate status

A current successful `lake build FormalResearch` has **not** yet been obtained.

On exact head `e73babebe4b2a97b79a0168e7052c0fd027d3472`, run `34058351961` entered `timeout 1200 lake build FormalResearch`. Endpoint14 and many other modules built successfully, but the aggregate surfaced pre-existing failures outside Endpoint14 and ultimately exited with code 124 at the 1200-second limit while Lake continued compiling independent targets.

Concrete aggregate blockers surfaced in that run include:

- `FormalResearch/QIA/PairingToPrintedBasis.lean`: noncomputable definitions and proof failures around square-root hypotheses;
- `FormalResearch/QIA/PrintedChiralitySpectrum.lean`: noncomputable definitions, unsupported `*ᵥ` notation under the pinned Lean/mathlib parser, and square-root proof failures;
- `FormalResearch/Blaschke/ZeroFlipWronskian.lean`: noncomputable definitions, unresolved `conj`, and a failing simp proof;
- `FormalResearch/Gaudin/CrossPoissonMatrixRank.lean`: missing/incorrect `Basis` availability and downstream rank proof failures;
- `FormalResearch/Gaudin/BernoulliInverseUniqueness.lean`: a changed `Finset.sum_congr` proof shape;
- `FormalResearch/QIB2/HammingRadialGate.lean`: normalization/rewrite failures;
- `FormalResearch/Bridges/B2BlaschkeWalshGauge.lean`: looping simp / max recursion depth;
- `FormalResearch/QIB1/ApolarKernelDimension.lean`: matrix cast/triangular determinant API mismatches;
- `FormalResearch/QIB1/ApolarCoherentKernel.lean`: finite-sum and rewrite shape failures;
- `FormalResearch/QIA/ThreeCopyAlgebraBinomialBridge.lean`: a distributivity rewrite no longer matches syntactically.

These are aggregate-maintenance blockers, not evidence against the Endpoint14 mathematics.

For aggregate promotion:

1. repair the unrelated compile blockers above in small module-level batches;
2. keep the Endpoint14-specific hard gate intact and green;
3. rerun `lake build FormalResearch` with no masked errors;
4. inspect exact-head Actions steps, not only the workflow badge;
5. re-audit Lean statements against current QIprojects source statements before promoting affected claims to `LEAN-CHECKED`.

## Blaschke author correction state

QIprojects contains the author-facing correction, not this Lean audit directory. Preserve the historical v13 wording but use the active convention

`U=(Re p)^2`, `V=(Im p)^2`.

The quartic downstream polynomial/sign/unsquaring algebra has a dedicated Lean module `FormalResearch/Blaschke/QuarticChargeGapAlgebra.lean`. Do not silently promote the degree-five Hodge-stability inequality; it remains open/numerical. Keep the analytic Bessel/model-space input and the raw charge-matrix reconstruction distinct from the downstream exact algebra.

## Repository cleanup / branch policy

- `apis.py` and `reasoning_agent.py`, inherited historical files unrelated to this project, were removed only from `formal-research-lean`.
- Default branch remains intentionally untouched.
- An old temporary branch `tmp-do-not-use` may still exist; it contains no unique research work and can be deleted when convenient.

## QIprojects synchronization

The former `QIprojects:formal-research-sync` branch is no longer the latest source snapshot: `QIprojects/main` has advanced since it was created. For this handoff a fresh branch `formal-research-handoff-2026-09-06` was created from current main. Use that branch for handoff-status edits rather than trying to continue the stale sync branch.

## Minimal restart checklist

Before new theorem work:

- fetch live `QIprojects/main` and `reasoner:formal-research-lean`;
- record both exact SHAs;
- read QIprojects project-local provenance/status/corrections for the target;
- read this handoff plus the exact Lean source, not `THEOREM_MAP.md` alone;
- inspect exact-head CI step results;
- classify the theorem as absent / formalized / module-green / aggregate-green / external;
- verify semantic agreement with the current QIprojects statement.

The immediate formal task is now: **preserve the green Endpoint14 bridge while repairing unrelated aggregate-maintenance blockers until `lake build FormalResearch` succeeds on an exact head.**

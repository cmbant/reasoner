name: Lean QI-D F4 exceptional facet

on:
  push:
    branches: [agents/qid-f4-exceptional-facet]
  workflow_dispatch:

jobs:
  qid-f4-exceptional:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - name: Install Lean toolchain via elan
        run: |
          curl https://elan.lean-lang.org/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve dependencies
        run: lake update
      - name: Fetch mathlib cache
        run: lake exe cache get
      - name: Reject placeholders
        run: |
          if grep -R --line-number -E '\bsorry\b|\badmit\b' FormalResearch FormalResearch.lean; then
            echo 'Lean placeholders found'
            exit 1
          fi
      - name: Build F4 exceptional finite core
        run: timeout 1500 lake build FormalResearch.QID.F4ExceptionalFacetCertificate

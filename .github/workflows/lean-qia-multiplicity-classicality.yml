name: Lean QIA multiplicity classicality

on:
  push:
    branches:
      - agents/qia-multiplicity-classicality

concurrency:
  group: lean-qia-multiplicity-classicality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Install Lean toolchain via elan
        run: |
          curl https://elan.lean-lang.org/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Resolve dependencies
        run: lake update
      - name: Fetch mathlib cache
        run: lake exe cache get
      - name: Reject placeholders
        run: |
          if grep -R --line-number -E '\bsorry\b|\badmit\b' FormalResearch FormalResearch.lean; then
            echo 'Lean placeholders found'
            exit 1
          fi
      - name: Build QIA multiplicity classicality module
        run: timeout 480 lake build FormalResearch.QIA.MultiplicityBlockClassicality
      - name: Build FormalResearch aggregate
        run: timeout 900 lake build FormalResearch
